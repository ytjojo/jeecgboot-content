# Decision Log — SDD 审核框架

## 2026-06-04 创建

### Pass 1 — Coherence 自检

**Spec Law Rule 1 (intent + success):** 7 个 Capability 均有 intent 和 success。通过。

**Spec Law Rule 2 (WHAT not HOW):** CAP-1 到 CAP-7 的 intent 均描述能力目标，未规定实现方式。通过。

**Spec Law Rule 3 (Constraints bend design):** 4 条 Constraint 均有实际约束力：
- 前后端独立审核通道 → 排除了"合并审核"的设计选项
- 已有 API 不可改签名 → 排除了"全面重构"的设计选项
- 可操作产出 → 排除了"泛泛建议"的设计选项
- RESTful 语义 → 排除了"RPC 风格"的设计选项
通过。

**Spec Law Rule 4 (Non-goals):** 4 条 Non-goal，明确排除了 SDD 生成、代码审查、业务需求、UI/UX 审核。通过。

**Spec Law Rule 5 (testable success signal):** 成功信号描述为"可直接并行开发"、"零非 RESTful 命名"、"零计划外 API"，均可验证。通过。

**Spec Law Rule 6 (stable unique IDs):** CAP-1 到 CAP-7，连续编号，无重复。通过。

**Spec Law Rule 8 (lean prose):** 内核散文精简，无装饰性文字。通过。

**发现的弱项:**
- CAP-3 的 success 中"零命名违规"可能过于严格，已有 API 的命名违规是否计入？→ 记入 assumptions: "已有 API 命名违规单独记录为技术债务，不计入本轮 CAP-3 判定"
- Open Questions 中有两个待确认问题，需用户决策

### Pass 2 — Preservation 自检

**负载信息落位:**
- 审核维度 → SPEC.md Capabilities（内核）
- 审核清单细节 → review-checklist.md（Companion）
- 审核工作流 → review-workflow.md（Companion）
- 指标和阈值 → review-metrics.md（Companion）
- API 命名规则 → api-naming-rules.md（Companion）

**Wrapper-only content:** 无。所有输入内容均已落位。

**信息完整性:** 用户输入的每个关键点均有对应落位：
- 前后端区分 → CAP-2/F系列 + CAP-3/B系列 + review-workflow.md 模式 B/C
- 后端已有实现 → CAP-5/B2 系列 + review-workflow.md 模式 B/C
- blackList 命名问题 → api-naming-rules.md 反面案例库
- 审核维度 → review-checklist.md L1-L2 + F1-F4 + B1-B4 + C1-C2
- 审核工作流 → review-workflow.md 7 个 Phase
- 验收标准 → review-metrics.md 门禁指标
- 确切指标 → review-metrics.md 4 级指标体系

### Verdict（初版）

SPEC.md 通过 Spec Law 8 条自审。7 个 Capability 均有 intent + success，4 条 Constraint 有实际约束力，4 条 Non-goal 明确排除范围。负载信息按 Companion 规则拆分为 4 个文件。2 个 Open Questions 待用户确认。

---

## 2026-06-04 更新（用户反馈）

### 变更内容

**用户决策:**
1. CRITICAL 问题 → 提示用户启动 subagent 修复规范文档（审核者不直接修改）
2. 审核完成后 → 提示用户启动 subagent 修复
3. 接口缺失/后端代码问题 → 提示用户启动 subagent 修复后端代码
4. 流程: 审核 → 确认 → 修复规范文档 → 修复依赖模块 → 再审核 → 通过才可开发

**变更:**
- SPEC.md: 新增 CAP-8（依赖分析），更新 CAP-6（增加 subagent 提示），新增 2 条 Constraint，更新 Success Signal，解决 Open Questions
- review-workflow.md: 重写为迭代审核流程，新增 Step 6 依赖分析、Step 8 用户提示、三类修复模板、"可开发"判定标准
- review-checklist.md: 新增 D1 前端依赖 + D2 后端依赖 + D3 优先级 + subagent 启动提示表
- review-metrics.md: 新增依赖满足指标，重写门禁判定规则为两步（规范审核 + 依赖检查）

### Verdict（更新后）

8 个 Capability，6 条 Constraint，4 条 Non-goal。审核流程从"一次性判定"改为"迭代修复直到可开发"。所有修复通过 subagent 执行，审核阶段保持独立。Open Questions 已全部解决。
