<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.content.circle.mapper.CircleAnnouncementMapper">

    <resultMap id="BaseResultMap" type="org.jeecg.modules.content.circle.entity.CircleAnnouncement">
        <id column="id" property="id"/>
        <result column="circle_id" property="circleId"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="expire_at" property="expireAt"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectActiveByCircleId" resultMap="BaseResultMap">
        SELECT * FROM circle_announcement
        WHERE circle_id = #{circleId}
          AND status = 'ACTIVE'
          AND (expire_at IS NULL OR expire_at > NOW())
        LIMIT 1
    </select>

</mapper>
