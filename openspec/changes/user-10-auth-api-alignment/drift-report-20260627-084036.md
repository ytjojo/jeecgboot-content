# OpenSpec 漂移检测+架构审核报告 — user-10-auth-api-alignment

**审核时间戳**: 20260627-084036  
**Change**: user-10-auth-api-alignment (认证安全API路径对齐)  
**审核人**: OpenSpec Audit Expert

---

## 一、代码-文档漂移总览

| 漂移类型 | 数量 | 说明 |
|---------|------|------|
| 前端验证任务未完成 | 5个 | 4.2-4.6均为前端验证，后端路径已对齐 |

---

## 二、API路径一致性检查（核心审核项）

### 全局硬规则验证
> **全局硬规则**: 内容社区 API 路径统一使用 `/api/v1/content/` 前缀，与基础库 `/sys/` 隔离，**禁止混用** `/api/v1/auth/`、`/api/v1/account-security/` 等无 `content/` 段的路径。

### 已验证Controller（全部符合规范）

| Controller | 路径 | 合规性 |
|-----------|------|--------|
| ContentAuthController | `/api/v1/content/auth` | ✅ 合规 |
| ContentRiskControlController | `/api/v1/content/account-security` | ✅ 合规 |
| ContentAccountCancellationController | `/api/v1/content/account-cancellation` | ✅ 合规 |

### 需进一步扫描确认
- 是否还有其他auth相关Controller未对齐？
- 是否存在硬编码的旧路径字符串在代码中（如Feign客户端调用、RedirectView等）？

---

## 三、架构分层审核

**POSITIVE**:
- auth相关Controller独立在content/auth包下
- 路径对齐严格遵循全局硬规则
- 纯重构类型change，风险可控
- 不涉及数据库变更，回滚简单

**WARNING**:
- 需确认是否存在旧路径残留（代码中硬编码字符串、配置文件、前端API常量等）
- 建议全量搜索 `/api/v1/auth`、`/api/v1/account-security` 等旧路径字符串确保无残留

---

## 四、安全审核

- 认证接口属于敏感接口，路径重构后需确认Security配置中对应的antMatchers也同步更新
- 建议检查SecurityConfig/ShiroConfig中的路径白名单和权限规则

---

## 五、门禁判定

✅ **PASS — 路径对齐核心目标已达成，架构合规**

1. ✅ 3个核心Controller路径均已对齐到 `/api/v1/content/` 前缀
2. ⚠️ 建议：全量搜索旧路径字符串确保无残留
3. ⚠️ 建议：检查Security配置中的路径规则同步更新
4. ⚠️ 建议：完成前端适配验证任务
