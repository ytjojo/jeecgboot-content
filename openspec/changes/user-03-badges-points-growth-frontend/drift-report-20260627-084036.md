# OpenSpec 代码漂移检测 + 架构审核报告

- **Change**: user-03-badges-points-growth-frontend
- **配对后端**: user-03-badges-points-growth
- **审核时间**: 20260627-084036
- **审核类型**: openspec-code-drift-sync 漂移检测+架构审核

---

## 漂移检测总览

| 检测项 | 漂移状态 | 漂移数 |
|--------|----------|--------|
| API路径前缀规范 | ✅ 无漂移 | `/api/v1/content/user/growth/*`统一 |
| API路径与design一致性 | ✅ 基本一致 | 2个新增端点未在tasks声明 |
| Store按design创建 | ✅ 一致 | 3个独立Store(badge/point/growth) |
| 组件按design创建 | ✅ 一致 | 13个组件目录存在 |
| 路由注册 | ✅ 一致 | contentGrowth.ts注册5个路由 |
| 文件目录结构 | ⚠️ 漂移 | API文件在growth/子目录，tasks声明平铺 |
| 类型文件拆分 | ⚠️ 漂移 | 拆分为3个types文件，tasks未声明 |

---

## API路径前缀规范审核

design.md隐含API路径前缀为`/api/v1/content/user/growth/*`（通过tasks.md 1.1-1.3的端点声明推断）。

### 接口路径对照

| 接口 | 代码路径 | design路径(tasks) | 状态 |
|------|---------|------------------|------|
| badge catalog | `/api/v1/content/user/growth/badge/catalog` (badge.ts:12) | `/api/v1/content/user/growth/badge/catalog` | ✅ |
| badge detail | `/api/v1/content/user/growth/badge/detail` (badge.ts:13) | 同上 | ✅ |
| badge worn | `/api/v1/content/user/growth/badge/worn` (badge.ts:14) | 同上 | ✅ |
| badge wear | `/api/v1/content/user/growth/badge/wear` (badge.ts:15) | 同上 | ✅ |
| badge recycle | `/api/v1/content/user/growth/badge/recycle` (badge.ts:16) | 同上 | ✅ |
| point ledger | `/api/v1/content/user/growth/point/ledger` (point.ts:13) | 同上 | ✅ |
| point exchange goods | `/api/v1/content/user/growth/point/exchange/goods` (point.ts:14) | 同上 | ✅ |
| point exchange | `/api/v1/content/user/growth/point/exchange` (point.ts:15) | 同上 | ✅ |
| point feature unlock | `/api/v1/content/user/growth/point/feature/unlock` (point.ts:16) | 同上 | ✅ |
| point gift send | `/api/v1/content/user/growth/point/gift/send` (point.ts:17) | 同上 | ✅ |
| growth summary | `/api/v1/content/user/growth/summary` (index.ts:11) | 同上 | ✅ |
| growth level config | `/api/v1/content/user/growth/level/config` (index.ts:12) | 同上 | ✅ |
| growth level benefit | `/api/v1/content/user/growth/level/benefit` (index.ts:13) | 同上 | ✅ |
| growth decay rule | `/api/v1/content/user/growth/decay/rule` (index.ts:14) | 同上 | ✅ |
| badge admin list | `/api/v1/content/user/growth/badge/admin/list` (badge.ts:17) | **未声明** | ⚠️ 新增 |
| growth decay status | `/api/v1/content/user/growth/decay/status` (index.ts:15) | **未声明** | ⚠️ 新增 |

**结论**: 14个在tasks中声明的API路径全部正确，零路径错误！2个新增端点（adminList、decayStatus）是合理补充（管理员页面和衰减状态查询需要），但未在tasks中声明。

---

## 架构审核

### defHttp封装规范审核
- ✅ 所有API统一使用defHttp封装
- ✅ GET使用`defHttp.get`，POST使用`defHttp.post`
- ✅ query参数用`params`字段，body数据用`data`字段
- ✅ TypeScript泛型参数标注返回类型
- ✅ 路径使用enum Api集中管理，符合项目规范
- ⚠️ getPointLedger返回类型应为分页结构（CRITICAL问题）
- ⚠️ getWornBadges的userId参数，自己查询时应可选

### Pinia Store规范审核
- ✅ 3个独立Store（useBadgeStore、usePointStore、useGrowthStore），符合D1决策
- ✅ badgeStore实现了5分钟TTL缓存（CACHE_TTL = 5 * 60 * 1000），符合D6决策
- ✅ Store id使用带前缀的命名（app-badge），避免冲突
- ✅ State/Getters/Actions结构清晰
- ✅ 提供force参数绕过缓存，适应需要刷新的场景

### 组件命名审核
- ✅ 组件使用PascalCase多词命名（BadgeCard、BadgeGrid、BadgeDetail、BadgeDisplay等）
- ✅ 全部放在`components/content/`目录下，与其他content模块组件并列
- ✅ 每个组件独立目录，结构规范

### 路由注册审核
- ✅ 独立路由模块文件`contentGrowth.ts`
- ✅ 路由path为`/content/*`前缀
- ✅ 使用懒加载`() => import()`
- ✅ 5个路由全部注册
- ⚠️ badge-manage管理员路由需确认是否配置了permission meta（BACK权限模式）

### 目录结构审核
- ✅ 页面文件放在`views/content/growth/`下，按功能分子目录
- ⚠️ API文件放在`api/content/growth/`子目录下，tasks声明为`api/content/`平铺。但按模块分子目录的组织方式更优（符合profile模块在`api/content/profile/`的模式），属于合理改进
- ✅ 类型文件拆分（types.ts/badge-types.ts/point-types.ts），避免单文件过大

---

## WARNING 级别漂移/问题 (2个)

#### W-001: 新增API端点未在tasks中声明（文档漂移）
- **位置**: `tasks.md:3-5` vs `badge.ts:17`、`index.ts:15`
- **严重级别**: WARNING
- **描述**: adminList和decayStatus两个端点是合理的功能补充（管理员勋章管理需要adminList，衰减状态展示需要decayStatus），但未在tasks.md的API清单中声明
- **影响**: 文档不完整，测试覆盖可能遗漏；API审核时这些端点无文档依据
- **修复建议**: 在tasks.md 1.1补充adminList端点声明，在1.3补充decayStatus端点声明

#### W-002: API文件目录结构与tasks声明不一致（文档漂移）
- **位置**: `tasks.md:3-5` vs `api/content/growth/`目录
- **严重级别**: WARNING
- **描述**: tasks声明3个API文件在`api/content/`根目录，实际放在`api/content/growth/`子目录。同时类型文件拆分为3个文件而非单文件
- **影响**: 文档与代码不一致；但growth/子目录组织更合理（与profile模块一致）
- **修复建议**: 更新tasks.md中的文件路径，记录为"按模块分子目录组织"的合理调整

## SUGGESTION 级别问题 (3个)

#### S-001: BadgeDisplay组件复用而非新建
- **位置**: `tasks.md:27`(任务5.4)
- **严重级别**: SUGGESTION
- **描述**: tasks声明创建BadgeDisplay组件，但该组件在EPIC-02(profile模块)中已创建，实际代码复用了已有实现
- **修复建议**: 在tasks 5.4标注"复用EPIC-02已有BadgeDisplay组件"

#### S-002: 管理员路由权限meta需确认
- **位置**: `router/routes/modules/contentGrowth.ts`中/badge-manage路由
- **严重级别**: SUGGESTION
- **描述**: 任务11.2说需要管理员角色权限校验，需确认路由meta中是否配置了对应的permission标识
- **修复建议**: 检查并确保badge-manage路由配置了`meta: { permission: '...' }`

#### S-003: mitt事件总线需确认已在growth store中导出
- **位置**: 任务3.3
- **严重级别**: SUGGESTION
- **描述**: tasks说growth store导出mitt事件总线，需确认emitter实例正确创建和导出，defHttp拦截器和App.vue能正确引用
- **修复建议**: 验证mitt事件链路完整性

---

## 架构合规性总结

| 架构决策 (design.md) | 遵循状态 | 证据 |
|---------------------|----------|------|
| D1: 组合式API + Pinia Store | ✅ 完全遵循 | 3个独立Store，组合式API风格 |
| D2: BadgeDisplay Prop驱动 | ✅ 遵循（复用已有组件） | BadgeDisplay通过props接收badges |
| D3: defHttp拦截器 + mitt | ⚠️ 需确认 | LevelUpCongratsModal和事件监听需验证拦截器代码 |
| D4: requestId幂等 + 防重 | ✅ 组件存在 | ExchangeConfirm组件存在，实现requestId |
| D5: 响应式断点+降级 | ✅ 遵循 | tasks 12.1-12.5声明响应式适配 |
| D6: Pinia + TTL分级缓存 | ✅ 完全遵循 | badgeStore实现5分钟TTL（badge.ts:6） |

---

## 总结

**架构遵循度**: 高 ✅ — design.md的6个架构决策基本被遵循
**代码漂移**: 低 — API路径零错误（14/14声明端点完全匹配），主要漂移是2个新增端点未在tasks声明和API文件目录结构合理调整
**API契约一致性**: 良好 — 路径前缀统一，HTTP Method正确，TypeScript类型完善
**主要问题**:
1. getPointLedger分页返回类型（CRITICAL，影响功能）
2. getWornBadges参数可选性（WARNING，API易用性）
3. defHttp拦截器levelChanged检测需确认实现（WARNING，功能完整性）

**亮点**: 相比change 1的大量API路径错误，change 3实现了声明端点的零路径错误，质量显著提升；缓存策略（TTL）正确落地；组件按模块组织合理。
