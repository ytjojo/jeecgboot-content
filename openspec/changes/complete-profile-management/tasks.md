## 1. 路由与页面骨架

- [ ] 1.1 在内容社区模块路由下新增 `/content/profile/edit`、`/content/profile/homepage-settings`、`/content/profile/privacy`、`/content/profile/history` 四个路由配置
- [ ] 1.2 创建编辑资料页骨架组件 `views/profile/edit/index.vue`，包含顶部导航栏、头像区域、表单区域、底部操作栏的基本布局
- [ ] 1.3 创建主页设置页骨架组件 `views/profile/homepage-settings/index.vue`，包含背景图设置区、主题色设置区、预览区的基本布局
- [ ] 1.4 创建隐私设置页骨架组件 `views/profile/privacy/index.vue`，包含说明区域、批量操作区域、字段可见性列表的基本布局
- [ ] 1.5 创建历史记录页骨架组件 `views/profile/history/index.vue`，包含标签页切换、当前值展示区、历史列表的基本布局

## 2. API 封装与类型定义

- [ ] 2.1 创建 `api/profile.ts`，封装资料管理接口（获取当前资料、更新资料、上传头像、上传背景图、查询修改次数、查询审核状态）
- [ ] 2.2 创建 `api/homepage.ts`，封装主页设置接口（获取主页配置、更新主页配置、更新模块配置、恢复默认主页）
- [ ] 2.3 创建 `api/verification.ts`，封装认证标识接口（获取用户认证标识、获取认证详情）
- [ ] 2.4 创建 `api/privacy.ts`，封装隐私设置接口（获取隐私设置、更新隐私设置、查询隐私修改次数）
- [ ] 2.5 创建 `api/profile-history.ts`，封装历史记录接口（获取昵称历史、获取头像历史、恢复历史值）
- [ ] 2.6 定义 TypeScript 类型：UserProfileVO、HomepageConfigVO、BadgeItem、PrivacySettingVO、HistoryRecordVO 等

## 3. 状态管理扩展

- [ ] 3.1 扩展 `useUserStore`，新增 profileCompletionRate、dailyUpdateRemaining、reviewStatus、reviewReason 状态字段
- [ ] 3.2 实现 useUserStore 中资料数据的刷新方法（隐私设置保存后调用）

## 4. 编辑资料页实现

- [ ] 4.1 实现编辑资料页表单，使用 Ant Design Vue Form 组件，包含昵称、简介、性别、生日、地区、职业、个人链接字段
- [ ] 4.2 实现表单校验逻辑：昵称必填（最大 20 字符）、简介字数统计（最大 500）、URL 格式校验、生日禁用未来日期
- [ ] 4.3 实现保存/取消按钮行为：PC 端顶部和底部按钮功能一致，移动端底部固定（适配 Safe Area），返回时 isDirty 检查
- [ ] 4.4 实现频率限制交互：页面加载时查询剩余次数显示提示条，达到上限时保存按钮置灰
- [ ] 4.5 实现审核状态交互：待审核时黄色 Alert + 字段不可编辑，审核不通过时红色 Alert + 重新编辑按钮
- [ ] 4.6 实现加载中骨架屏、网络错误提示、防重复提交等通用状态处理

## 5. 头像上传裁剪组件

- [ ] 5.1 创建 `AvatarCropper` 组件，集成 cropperjs 库，实现 1:1 比例裁剪、滚轮缩放、滑块缩放
- [ ] 5.2 实现文件校验逻辑：格式校验（JPG/PNG/WebP）、大小校验（≤5MB）、非空校验
- [ ] 5.3 实现裁剪弹窗 UI：图片预览区、缩放控制条、圆形/方形预览、底部操作按钮
- [ ] 5.4 实现上传流程：裁剪完成后上传、显示进度、成功更新头像预览、失败提示重试
- [ ] 5.5 实现移动端适配：全屏展示裁剪区域，裁剪预览区底部横向排列

## 6. 主页设置页实现

- [ ] 6.1 实现背景图上传功能，复用 AvatarCropper 组件（16:9 比例），支持更换背景和恢复默认
- [ ] 6.2 实现主题色选择功能：预设色板（8-12 个颜色）网格排列，支持自定义颜色输入（ColorPicker）
- [ ] 6.3 实现主题色对比度校验：WCAG AA 标准自动校验，对比度不足时警告提示并自动调整文字颜色
- [ ] 6.4 实时预览区：展示背景图 + 主题色效果，PC 端右侧固定预览，移动端折叠后 Drawer 全屏预览
- [ ] 6.5 实现保存和恢复默认功能

## 7. 主页模块配置实现

- [ ] 7.1 安装 vuedraggable 依赖，创建模块排序列表组件
- [ ] 7.2 实现模块显隐 Switch 开关，关闭时模块从主页隐藏
- [ ] 7.3 实现拖拽排序功能：PC 端拖拽手柄，移动端长按（300ms）触发拖拽模式
- [ ] 7.4 实现移动端拖拽交互优化：触摸热区 44px、阴影反馈、蓝色插入线、振动反馈
- [ ] 7.5 实现键盘无障碍操作：上下箭头键调整顺序、Enter 确认、Escape 取消
- [ ] 7.6 实现全部隐藏校验、未保存离开确认、恢复默认排序功能

## 8. 认证标识组件实现

- [ ] 8.1 创建 `VerificationBadge` 组件，Props 接收 badges 数组，内部处理排序和折叠逻辑
- [ ] 8.2 实现 7 种认证类型的 Badge 样式（颜色、图标、Tooltip）
- [ ] 8.3 实现认证详情弹窗：PC 端 Modal（400px），移动端全屏 Drawer，展示认证类型、时间、说明
- [ ] 8.4 实现折叠交互：最多显示 2 个标识 + "+N" 徽标，点击展开显示全部
- [ ] 8.5 实现昵称截断策略：PC 端 200px、移动端 120px 截断，认证标识紧跟显示不换行

## 9. 隐私设置页实现

- [ ] 9.1 实现字段可见性列表：每个字段右侧 Select 组件，支持四种可见性级别（公开/仅关注者/互关可见/仅自己）
- [ ] 9.2 实现昵称和头像不可修改置灰状态 + Tooltip 说明
- [ ] 9.3 实现默认可见性设置：修改后仅影响新字段
- [ ] 9.4 实现"一键全部设为"批量操作：确认弹窗 + 批量更新 + 字段高亮 2 秒 + 5 秒撤销窗口
- [ ] 9.5 实现隐私修改频率限制：每小时 10 次，超限后保存按钮禁用 + 黄色提示条
- [ ] 9.6 实现保存成功后主动刷新本地缓存，提示"新设置将立即对新访问者生效"
- [ ] 9.7 实现移动端适配：Select 改为 ActionSheet 底部选择器

## 10. 历史记录页实现

- [ ] 10.1 实现 Tabs 组件切换昵称/头像历史
- [ ] 10.2 实现当前值展示区：昵称/头像 + 绿色"当前"标签，头像高亮蓝色边框
- [ ] 10.3 实现历史记录列表：倒序排列，每条显示旧值 + 修改时间 + "恢复"按钮
- [ ] 10.4 实现恢复操作：确认弹窗 → 调用恢复接口 → 成功提示 → 处理昵称冲突和频率限制
- [ ] 10.5 实现空状态、加载中骨架屏、恢复中 loading 状态

## 11. 响应式适配与通用交互

- [ ] 11.1 实现所有页面的响应式断点适配（xs/sm/md/lg/xl），PC 端居中 640px，移动端全宽
- [ ] 11.2 实现通用交互：表单实时校验（防抖 300ms）、防重复提交、危险操作确认弹窗、成功消息提示
- [ ] 11.3 实现 iPhone Safe Area 适配：底部操作栏 padding-bottom: env(safe-area-inset-bottom)
- [ ] 11.4 实现认证标识弹窗的响应式切换：PC 端 Modal，移动端 Drawer

## 12. 测试与验证

- [ ] 12.1 编写编辑资料页单元测试：表单校验、频率限制、审核状态展示
- [ ] 12.2 编写头像裁剪组件单元测试：文件校验、裁剪参数、上传流程
- [ ] 12.3 编写认证标识组件单元测试：排序逻辑、折叠策略、详情弹窗
- [ ] 12.4 编写隐私设置页单元测试：可见性设置、批量操作、撤销功能
- [ ] 12.5 编写历史记录页单元测试：恢复操作、频率限制、空状态
- [ ] 12.6 全量测试运行，确保 100% 通过
