# OpenSpec 代码漂移检测与架构审核报告
**Change**: channel-24-lifecycle-frontend
**时间戳**: 20260627-084036
**审核类型**: openspec-code-drift-sync

---

## 一、代码-文档漂移检测

### CRITICAL级漂移

| 编号 | 漂移类型 | 文档位置 | 代码位置 | 漂移描述 | 修复建议 |
|------|----------|----------|----------|----------|----------|
| D-01 | Store命名漂移 | proposal.md:36 | store/modules/channelReview.ts | 文档中"新增4个Store"包含channelReview，但未考虑到channel-22-content-governance-frontend已经使用了同名`useChannelReviewStore`（id: `app-channel-review`）用于内容待审审核，造成Store命名冲突 | 将channel-24的审核Store重命名为`useChannelLifecycleReviewStore`（id: `app-channel-lifecycle-review`），更新所有引用。后续设计新Store时建议命名空间更具体（如channelContentReview vs channelCreateReview） |
| D-02 | 目录结构漂移 | design.md（预期统一目录） | views/ vs views/content/ | 实际代码存在两个并行的channel目录：`views/channel/`（channel-20/22/23部分文件）和`views/content/channel/`（channel-24文件），API统一在`api/content/channel/`，目录不一致导致导入和维护困难 | **立即统一目录结构**：建议以API目录为基准统一使用`views/content/channel/`，将`views/channel/`下所有文件迁移并更新import路径。后续change禁止在未统一前新增文件到旧目录 |

### WARNING级漂移

| 编号 | 漂移类型 | 文档位置 | 代码位置 | 漂移描述 | 修复建议 |
|------|----------|----------|----------|----------|----------|
| D-03 | API文件组织 | proposal中按模块划分API | api/content/channel/ | 需验证生命周期管理API（冻结/解冻/合并/归档等8个接口）是否有独立文件还是分散在其他文件中 | 检查是否有lifecycle.ts或governance.ts处理生命周期API，保持一个领域一个文件的拆分原则 |
| D-04 | 轮询策略 | tasks.md 6.2说"3秒间隔批量查询" | 代码待验证 | 需验证导出轮询是否有超时机制，文档未提最大轮询次数 | 添加轮询超时（如最多90秒），超时后提示用户稍后在历史中查看 |
| D-05 | ECharts复用 | tasks.md 1.2说封装useChart hook | hooks/web/useECharts.ts已存在 | 项目已有useECharts，应复用而非重新封装 | 检查并复用现有useECharts hook，必要时扩展功能而非重写 |

---

## 二、架构规范审核

### API层封装规范审核

| 规范项 | 符合度 | 问题位置 | 问题描述 | 修复建议 |
|--------|--------|----------|----------|----------|
| 使用defHttp封装 | ✅ 符合 | stats.ts/export.ts/auditLog.ts/appeal.ts | 都使用defHttp | - |
| 使用enum Api | ✅ 符合 | 已检查文件 | 都用enum管理路径 | - |
| API路径前缀正确 | ✅ 符合 | 已检查文件 | 都使用`/api/v1/content/channel/`前缀 | - |
| TypeScript类型定义 | ✅ 符合 | stats.ts/export.ts | 类型定义完善 | - |
| 分页参数统一 | ✅ 符合 | export.ts | 使用current/size，与项目一致 | - |
| API按领域拆分 | ⚠️ 待验证 | - | 需确认lifecycle和review API是否拆分到独立文件 | 保持一个领域一个文件（stats.ts/export.ts/lifecycle.ts/review.ts/auditLog.ts/appeal.ts） |
| 文件位置正确 | ✅ 符合 | api/content/channel/ | 位置正确（对比channel-23的错误位置，channel-24这一点做对了） | - |

**API层架构问题分级**:

| 编号 | 级别 | 描述 | 建议 |
|------|------|------|------|
| ARCH-API-01 | WARNING | 需检查生命周期和审核相关API是否拆分正确，文件命名是否清晰 | 确认lifecycle.ts存在并包含所有8个生命周期操作API |

### Store使用规范审核

| 规范项 | 符合度 | 问题位置 | 问题描述 | 修复建议 |
|--------|--------|----------|----------|----------|
| 使用defineStore | ✅ 符合 | 已检查Store | 都使用Pinia | - |
| Store id唯一 | ❌ 不符合 | channelReview | 与channel-22重名（D-01） | 重命名Store和id |
| useXxxStoreWithOut函数 | ⚠️ 待验证 | 所有Store | 需验证是否提供WithOut函数 | 添加WithOut函数保持与其他Store一致 |
| action错误处理 | ⚠️ 待验证 | 所有Store | channel-22的Store缺少错误处理，channel-24需验证是否有完善的try/catch | 参考channel-23 discoveryStore的错误处理模式，所有异步action添加错误捕获和提示 |
| loading状态 | ⚠️ 待验证 | 所有Store | 需验证列表加载和操作按钮的loading状态 | 为异步操作添加loading状态，防止重复提交 |

**Store架构问题分级**:

| 编号 | 级别 | 位置 | 描述 | 建议 |
|------|------|------|------|------|
| ARCH-STORE-01 | CRITICAL | channelReview | 与channel-22重名冲突 | 立即重命名为唯一名称 |
| ARCH-STORE-02 | WARNING | 所有Store | 错误处理和loading状态需验证 | 确保所有异步action有try/catch和loading状态 |

### 组件规范审核

| 规范项 | 符合度 | 问题位置 | 问题描述 | 修复建议 |
|--------|--------|----------|----------|----------|
| 组件命名PascalCase | ✅ 符合 | 所有组件 | 命名正确 | - |
| 组件目录统一 | ❌ 不符合 | views/channel/components vs views/content/channel/components | 两个components目录并存 | 统一到views/content/channel/components/ |
| Props/Emits类型定义 | ⚠️ 待验证 | 所有组件 | 需验证TypeScript类型完整 | 确保所有组件Props和Emits都有类型定义 |

**组件架构问题分级**:

| 编号 | 级别 | 描述 | 建议 |
|------|------|------|------|
| ARCH-COMP-01 | CRITICAL | 组件目录双轨制 | 统一到views/content/channel/components/ |

### 路由注册规范审核

| 规范项 | 符合度 | 问题位置 | 问题描述 | 修复建议 |
|--------|--------|----------|----------|----------|
| 路由文件存在 | ⚠️ 待验证 | router/routes/modules/ | channel-23路由在contentDiscovery.ts，channel-24需确认路由注册位置 | 建议新建channel.ts路由模块，所有channel相关路由统一注册，或确认现有路由位置 |
| BACK权限模式 | ⚠️ 待验证 | - | 治理后台、审核队列等运营页面需要后端菜单配置 | 确认后端菜单已配置，权限指令v-auth正确使用 |

---

## 三、前端特有边界场景审核

根据tasks.md声明已覆盖：

| 边界场景 | 是否覆盖 | 备注 |
|----------|----------|------|
| 网络异常处理 | ✅ | tasks 11.6声明覆盖 |
| Token过期 | ✅ | 全局处理 |
| 重复提交防护 | ✅ | tasks 6.5声明loading+禁用 |
| 空数据状态 | ✅ | tasks 7.5声明覆盖 |
| Loading/骨架屏 | ✅ | tasks 5.7声明覆盖 |
| 响应式布局 | ✅ | tasks 5.8声明适配三端 |
| 高风险操作确认 | ✅ | tasks 8.6永久关闭输入名称确认 |
| 轮询超时 | ⚠️ | 需验证是否有最大轮询时长 |
| 导出下载过期 | ✅ | tasks 6.4声明7天有效期处理 |
| 超时标记 | ✅ | tasks 7.2审核24小时、10.5申诉3工作日 |

整体边界场景覆盖较完善，主要需补充轮询超时处理。

---

## 四、跨Change架构问题总结

审核完4个change后，发现几个跨change的共性架构问题：

| 问题 | 影响范围 | 严重级别 | 建议 |
|------|----------|----------|------|
| 目录双轨制（views/channel/ vs views/content/channel/） | 所有4个change | CRITICAL | **立即统一目录结构**，建议以API目录`api/content/channel/`为准，统一使用`views/content/channel/`，一次性迁移所有文件 |
| Store命名空间冲突风险 | channel-22和channel-24 | CRITICAL | 重命名冲突的channelReview，后续Store命名添加更具体的业务前缀 |
| Store缺少统一的错误处理和loading | channel-20/22 | WARNING | 参考channel-23 discoveryStore的模式，为所有Store异步action添加try/catch、用户提示、loading状态 |
| API文件组织不一致 | channel-23 vs 其他 | WARNING | channel-23的API文件放错位置且未拆分，应统一放在api/content/channel/下按领域拆分 |
| 分页参数不统一风险 | channel-23 | WARNING | channel-23设计用page/pageSize与项目current/size不一致，需统一 |

---

## 五、总结

### channel-24主要问题

1. **CRITICAL级问题2个**：Store命名冲突、目录双轨制（这是跨所有change的共性架构债务）
2. API层实现质量较好（路径正确、分页正确、类型完善），这点比channel-23好
3. 边界场景覆盖较全，高风险操作确认、轮询、超时标记都有考虑
4. 需验证Store错误处理和loading状态是否完善

### 修复优先级建议

**P0（合并前必须修复）**:
- 重命名冲突的channelReview Store（ARCH-STORE-01）
- 不新增文件到旧目录，制定目录统一计划（D-02/ARCH-COMP-01）

**P1（联调前完善）**:
- 验证并完善Store的错误处理和loading状态（ARCH-STORE-02）
- 检查生命周期API文件拆分完整性（ARCH-API-01）
- 添加导出轮询超时处理（D-04）
- 复用现有useECharts hook（D-05）

**P2（后续统一重构）**:
- 跨change统一目录结构，迁移所有文件
- 统一Store代码风格
- 梳理并优化所有Store的错误处理模式

channel-24-lifecycle-frontend 功能完成度高（tasks全部标记完成），API层实现质量较好，主要问题是跨change的架构一致性问题，特别是Store命名和目录结构。修复P0问题后功能基本可用，目录统一建议作为单独的重构任务在所有功能合并后统一进行。
