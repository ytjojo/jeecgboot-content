# OpenSpec 规范文档审核报告
**Change**: channel-24-lifecycle-frontend
**时间戳**: 20260627-084036
**审核类型**: openspec-review-change

---

## 一、总体评分

| 维度 | 得分 | 说明 |
|------|------|------|
| 完整性 | 8/10 | proposal/design/specs/tasks齐全，能力覆盖全面 |
| 一致性 | 7/10 | 存在Store命名冲突风险，目录结构不一致 |
| 可实现性 | 9/10 | ECharts已有项目集成，组件和Store设计合理 |
| 可测试性 | 8/10 | 测试任务明确，边界场景有考虑 |
| 接口契约 | 7/10 | 后端依赖问题需确认，约29个API需后端对齐 |
| 边界覆盖 | 8/10 | 高风险操作确认、导出轮询、超时标记都有考虑 |
| API命名规范 | 8/10 | API路径RESTful风格基本一致 |
| 存量API兼容 | 8/10 | 需注意Store重名问题 |
| 跨端一致性 | 7/10 | 需核对后端API字段和状态码 |
| 依赖分析 | 8/10 | ECharts依赖说明，复用现有组件 |
| Plan审核 | N/A | schema为spec-driven，无plan.md |

**综合得分**: 7.8/10

---

## 二、问题清单

### BLOCK级问题（阻塞）

| 编号 | 位置 | 问题描述 | 影响 | 修复建议 |
|------|------|----------|------|----------|
| B-01 | proposal.md:36 vs 现有代码 | Store命名冲突：proposal中`channelReview` Store与channel-22-content-governance-frontend的`useChannelReviewStore`重名！channel-22是"内容待审审核"，channel-24是"频道创建/修改审核"，两个完全不同的业务域用同一个Store id | Pinia Store id冲突，状态互相覆盖，运行时数据混乱 | 将channel-24的审核Store重命名为`useChannelLifecycleReviewStore`或`useChannelAuditQueueStore`，id改为`app-channel-lifecycle-review`或类似唯一名称 |
| B-02 | 代码目录结构 | 目录双轨制：channel-22/23部分组件在`views/channel/`，channel-24部分页面在`views/content/channel/`，两个并行目录结构 | 导入路径混乱，维护困难，新人不知道代码在哪 | 统一channel根目录：建议明确所有channel相关代码统一放在`views/content/channel/`（与API目录`api/content/channel/`对应），将`views/channel/`下的内容迁移过来 |

### FLAG级问题（需关注）

| 编号 | 位置 | 问题描述 | 影响 | 修复建议 |
|------|------|----------|------|----------|
| F-01 | proposal.md:38 | 新增ECharts依赖，需确认项目是否已集成ECharts（design中未说明已有还是新增） | 可能重复安装或打包体积过大 | 检查项目已有ECharts依赖（utils/lib/echarts.ts存在），复用现有封装，按需引入 |
| F-02 | tasks.md 2.4 | 生命周期管理API封装说"11个接口"，但proposal中各模块API相加是5+4+3+8+2+2+1+4=29个，总数需核对 | API数量不匹配可能导致遗漏 | 核对API清单，确保所有接口都有封装 |
| F-03 | backend-issues.md | 需要读取backend-issues.md确认后端缺失API情况 | 可能存在类似channel-23的后端依赖问题 | 阅读backend-issues.md，与后端对齐缺失接口排期 |
| F-04 | tasks.md 6.2 | 导出任务轮询"3秒间隔批量查询"，未说明最大轮询时长和超时处理 | 任务一直处理中时前端无限轮询 | 补充轮询超时机制（如最多轮询10分钟，超时提示"任务处理中，请稍后在导出历史查看"） |
| F-05 | tasks.md 8.8 | 合并操作"含影响范围预览"，但spec中需明确预览展示什么信息（成员数、内容数、订阅数等） | 实现时信息展示不完整 | 在spec中明确合并预览的字段清单 |
| F-06 | tasks.md 8.6 | 永久关闭需"输入频道名称确认"，但需确认是否还有二次弹窗或更高级别权限校验 | 高风险操作误触风险 | 除了输入名称确认，建议增加管理员权限二次验证或操作日志记录 |

### ADVISORY级问题（建议）

| 编号 | 位置 | 问题描述 | 影响 | 修复建议 |
|------|------|----------|------|----------|
| A-01 | tasks.md 1.2 | useChart hook，项目已有hooks/web/useECharts.ts，建议复用而非重新封装 | 重复造轮子 | 检查现有useECharts是否满足需求，复用或扩展现有hook |
| A-02 | - | 高风险操作（冻结、永久关闭、合并、归档）建议都添加操作原因输入框，便于审计 | 审计日志缺少原因 | 除了永久关闭，其他高风险操作也建议支持选填或必填操作原因 |
| A-03 | - | 导出文件下载建议添加进度提示，大文件导出下载时用户体验更好 | 下载无反馈 | 下载时添加loading或进度提示 |

---

## 三、跨端一致性检查

1. **API路径前缀**：API设计使用`/api/v1/content/channel/`前缀，符合全局硬规则
2. **分页参数**：从export.ts看使用current/size，与项目惯例一致 ✅
3. **状态值映射**：需与后端对齐频道生命周期状态枚举（正常/冻结/隐藏/限制推荐/归档/关闭等）
4. **Store命名冲突**是最大跨change问题，必须解决（B-01）

---

## 四、总结

channel-24-lifecycle-frontend 功能覆盖完整，从数据看板到生命周期管理到申诉都有涉及，API和组件设计基本合理。**最严重的问题是Store命名冲突（B-01）和目录双轨制（B-02）**，这两个是架构级问题会影响整个channel模块的代码组织和运行时稳定性，必须在合并前修复。其他问题主要是后端依赖对齐和体验优化。整体文档质量较好，修复BLOCK级问题后可进入联调。
