# channel-24-lifecycle-stats 规范审核报告

**审核时间**: 2026-06-27 08:40:36
**Change类型**: 后端 (use-tdd-plan)
**审核范围**: proposal.md, design.md, plan.md, specs/ (9个spec), tasks.md

---

## 一、维度评分

| 维度 | 得分 | 问题数 | 主要问题 |
|------|------|--------|----------|
| 1. 完整性 | 6/10 | 4 | 9个specs覆盖面广，但API契约缺失错误码和鉴权定义 |
| 2. 一致性 | 6/10 | 3 | 表名前缀又回到channel_无content_，与channel-23不一致 |
| 3. 可实现性 | 7/10 | 3 | 功能范围大（9个能力），依赖EPIC-20/21/22/23较多，依赖未就绪风险 |
| 4. 可测试性 | 6/10 | 3 | Scenario数量多，但状态机流转、合并事务边界测试场景不明确 |
| 5. 接口契约 | 4/10 | 6 | specs未完整定义REST API路径/方法，导出/合并等复杂接口契约不足 |
| 6. 边界覆盖 | 5/10 | 7 | 合并事务回滚、并发冻结、超大导出OOM、归档后数据一致性、违规处罚申诉边界等缺失 |
| 7. API命名规范 | 4/10 | 3 | 路径使用单数（/channel/stats），资源嵌套不规范（应在/channels/{id}/下） |
| 8. 存量API兼容 | 7/10 | 1 | 未发现明显路径冲突 |
| 9. 跨端一致性 | 5/10 | 2 | 未找到明确配对的frontend change |
| 10. 依赖分析 | 5/10 | 4 | 强依赖EPIC-20/21/22/23，但前置change可能未完全就绪（如channel-22大部分功能未完成） |
| 11. 数据表审核 | 4/10 | 5 | 表名无content_前缀回到channel_，缺少软删除，统计汇总表索引和分表策略不明确 |
| 12. Plan审核 | 5/10 | 4 | Plan覆盖广但粒度粗，TDD顺序和分层实现不清晰，缺少大事务（合并）拆分策略 |

**综合得分**: 64/120 = **53.3%**

---

## 二、问题清单

### BLOCK级问题 (必须修复，门禁不通过)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| BLOCK-01 | 一致性 | tasks.md:3, 实体命名 | **表名前缀再次不一致**：本change回到`channel_stats`等无前缀命名，而channel-23正确使用了`content_`前缀 | 全仓库表名混乱，无统一规范 | 统一使用`content_`前缀，表名改为`content_channel_stats`等 |
| BLOCK-02 | 接口契约 | specs/* (9个spec) | **API契约严重不足**：9个spec均未明确定义HTTP method/path/req/vo完整契约 | 前后端无法对齐，多团队协作无依据 | 每个spec补充核心API契约表格 |
| BLOCK-03 | 可实现性 | proposal:60 | **强依赖前置EPIC但前置未完成**：依赖EPIC-22内容治理、EPIC-23发现可见性，但channel-22审核显示80%功能未实现 | 本change开发阻塞，或需要大量假设/临时逻辑 | 明确前置依赖完成标准，或在本change中实现依赖的最小必要子集 |

### FLAG级问题 (建议修复，不阻塞但有风险)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| FLAG-01 | 数据表审核 | 统计汇总表设计 | **channel_stats统计汇总表无索引和过期策略说明** | 大数据量下查询性能差，存储膨胀 | 添加时间维度索引，明确数据保留策略（如保留1年）和定时清理 |
| FLAG-02 | 边界覆盖 | channel-merge spec | **频道合并事务边界未定义**：内容/订阅/权限迁移是大事务还是补偿机制？失败如何回滚？ | 合并中途失败导致数据不一致，用户看到半合并状态 | 明确合并采用补偿事务（Saga模式），定义每步失败的回滚策略 |
| FLAG-03 | 边界覆盖 | channel-data-export spec | **超大导出无内存保护说明**：大频道数据导出可能OOM | 导出功能拖垮服务，影响可用性 | 明确异步导出+分页查询+SXSSF流式写入Excel，限制单次导出数据量 |
| FLAG-04 | 数据表审核 | 所有表 | **缺少软删除del_flag字段** | 误删审核记录/申诉无法恢复 | 所有表添加软删除字段 |
| FLAG-05 | 边界覆盖 | 生命周期状态机 | **状态非法流转拦截规则不完整**：哪些状态→哪些状态是允许的？归档能否直接解冻？合并能否撤销？ | 数据一致性问题，非法状态数据污染 | 补充完整状态流转矩阵，明确每个状态的合法前序状态 |

### ADVISORY级问题 (建议改进)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| ADV-01 | Plan审核 | tasks.md | **9个能力50+任务粒度过粗**，多项任务（如"实现合并执行逻辑"）是大功能点 | 开发进度难追踪，TDD难落地 | 拆分大任务为可验证的小任务 |
| ADV-02 | 性能 | channel-stats-dashboard | **P99 <= 1秒无缓存/预聚合说明** | 实时统计大频道查询超时 | 明确5分钟预聚合+缓存策略，禁止实时COUNT大表 |
| ADV-03 | API设计 | 现有Controller | ChannelStatsController使用`@RequestParam channelId`而非路径参数`/channels/{channelId}/stats` | REST资源嵌套不规范 | 改为路径参数方式`/api/v1/content/channels/{channelId}/stats/core` |

---

## 三、量化指标

| 指标 | 值 | 说明 |
|------|-----|------|
| PRD AC覆盖率 | 60% | Success Criteria共6项，约4项有Scenario覆盖 |
| API契约完整率 | 15% | 9个spec仅0个有完整API契约（现有Controller代码有但文档未同步） |
| 边界条件覆盖率 | 40% | 核心场景覆盖，大事务/并发/性能/归档一致性等边界缺失 |
| TDD配对率 | 50% | tasks标记的5项Biz测试，代码中存在Stats相关测试但其他测试需确认 |
| Scenario完整率 | 55% | 9个spec约40个Scenario，部分缺少前置条件和失败case |
| 数据表合规率 | 30% | 5张表中0张完全符合命名/软删除/索引规范 |

---

## 四、门禁判定

**❌ BLOCKED (不通过)**

**阻断原因**:
1. 表名前缀再次出现不统一（channel_ vs content_），违反仓库一致性
2. 9个specs API契约几乎空白
3. 强依赖前置EPIC-22未完成，本change开发基础不牢

**最小修复清单**:
1. 统一所有表名使用`content_`前缀
2. 为核心API（至少统计查询、审核操作、冻结/解冻、导出）补充契约
3. 明确前置依赖清单和最小依赖子集，避免因前置未完成完全阻塞
4. 补充频道合并的事务策略和回滚方案
5. 补充状态机合法流转矩阵
