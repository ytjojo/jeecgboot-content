# channel-23-discovery 规范审核报告

**审核时间**: 2026-06-27 08:40:36
**Change类型**: 后端 (use-tdd-plan)
**审核范围**: proposal.md, design.md, plan.md, specs/ (7个spec), tasks.md

---

## 一、维度评分

| 维度 | 得分 | 问题数 | 主要问题 |
|------|------|--------|----------|
| 1. 完整性 | 7/10 | 3 | specs章节相对完整，API契约仍有缺失，缺少错误码定义 |
| 2. 一致性 | 6/10 | 4 | design有重复Decision章节(D1出现两次)，plan.md与实际文件名不一致 |
| 3. 可实现性 | 7/10 | 2 | 技术选型明确，推荐/搜索策略有第一阶段明确方案 |
| 4. 可测试性 | 7/10 | 2 | Test Strategy详细，Scenario相对完整，部分缺少Given前置 |
| 5. 接口契约 | 5/10 | 5 | API路径/方法在specs中仍不够明确，错误码未定义 |
| 6. 边界覆盖 | 6/10 | 5 | 缺少搜索性能边界(P99保障措施)、推荐冷启动极端场景、分类循环引用检测 |
| 7. API命名规范 | 3/10 | 4 | 路径使用单数(/channel/category)，RPC风格动作(/create, /update, /disable)，POST用于更新/删除 |
| 8. 存量API兼容 | 7/10 | 2 | 未发现与现有API路径冲突 |
| 9. 跨端一致性 | 5/10 | 2 | 未找到明确的frontend配对change（有circle-12/13但需确认对应关系） |
| 10. 依赖分析 | 7/10 | 2 | 明确引用了ContentUserFollowRecommendation等已有模式，但ChannelVisibilityService依赖的频道状态未与EPIC-20对齐 |
| 11. 数据表审核 | 7/10 | 3 | 表名使用正确content_前缀，字段规范，但Flyway版本号不规范，缺少软删除，缺少回滚脚本验证 |
| 12. Plan审核 | 6/10 | 3 | Plan覆盖大部分Requirement，TDD顺序基本正确，但SQL文件名与design不一致 |

**综合得分**: 73/120 = **60.8%**

---

## 二、问题清单

### BLOCK级问题 (必须修复，门禁不通过)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| BLOCK-01 | API命名规范 | ContentChannelCategoryController.java:19,32,38,45,52 | **API路径使用单数+RPC风格**：`/api/v1/content/channel/category/create`、`/update`、`/disable`使用POST，不符合RESTful规范 | API风格与社区/行业标准不一致，维护成本高 | 改为复数资源：`GET /api/v1/content/channels/categories/tree`、`POST /categories`、`PUT /categories/{id}`、`PATCH /categories/{id}/status` |
| BLOCK-02 | 接口契约 | specs/* (7个spec) | **API路径和HTTP method未在specs中明确定义** | 前后端对齐无明确依据 | 在每个spec中补充API契约表格（method/path/req/vo/错误码） |
| BLOCK-03 | 一致性 | design.md:32-42, design.md:44-61 | **Decision章节重复定义**：D1"分类树存储方案"出现两次，内容有差异 | 文档阅读混淆，实现依据不明确 | 合并重复的Decision D1，删除冗余章节 |

### FLAG级问题 (建议修复，不阻塞但有风险)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| FLAG-01 | 数据表审核 | design.md:207 | **Flyway版本号命名不规范**：V3.9.1_63__不符合现有项目V1/V2递增模式 | 迁移脚本执行顺序混乱 | 改为V3__channel_discovery_tables.sql（按现有V1/V2序号递增） |
| FLAG-02 | 数据表审核 | design.md表结构定义 | **所有表缺少del_flag软删除字段** | 与JeecgBoot软删除模式不一致，数据无法恢复 | 所有表添加del_flag TINYINT(1) DEFAULT 0 |
| FLAG-03 | 边界覆盖 | specs/channel-search | **搜索P99 <= 200ms无保障措施**：仅说使用FULLTEXT+分页+缓存，无降级策略 | 高并发下性能无法保证 | 添加查询超时、空结果缓存、索引提示等保障 |
| FLAG-04 | 可实现性 | design.md:247 | **3个Open Question未给出最终决策** | 推荐口径/精选差异化/活跃度扣分规则实现无依据 | 与产品确认后更新design |
| FLAG-05 | 数据表审核 | design.md:55 | **path字段无长度校验逻辑说明** | 分类深度4级限制无实现约束 | 补充path字段长度校验和层级循环引用检测 |

### ADVISORY级问题 (建议改进)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| ADV-01 | 完整性 | specs/channel-rankings | **榜单计算公式权重(0.4/0.3/0.3)硬编码在文档中，无可配置说明** | 运营调整权重需改代码 | 说明权重配置方式（配置文件/数据库） |
| ADV-02 | 跨端一致性 | openspec/changes/ | **需确认circle-12-analytics-discovery是否为本change的前端配对** | 前后端字段对齐需确认 | 明确配对change关系 |
| ADV-03 | 可测试性 | Test Strategy | **定时任务测试缺少分布式锁场景** | 多实例部署时榜单可能重复计算 | 添加分布式锁并发测试 |

---

## 三、量化指标

| 指标 | 值 | 说明 |
|------|-----|------|
| PRD AC覆盖率 | 75% | Success Criteria共7项，5项有Scenario覆盖 |
| API契约完整率 | 40% | 7个spec仅3个有部分API路径描述，无完整req/vo/错误码 |
| 边界条件覆盖率 | 55% | 核心边界覆盖，缺少性能/并发/降级场景 |
| TDD配对率 | 80% | tasks标记的18项测试任务，实际存在17个测试文件 |
| Scenario完整率 | 70% | 共约30个Scenario，部分缺少Given前置 |
| 数据表合规率 | 75% | 7张表中5张符合命名/字段规范，缺软删除 |

---

## 四、门禁判定

**⚠️ CONDITIONAL PASS (有条件通过)**

**通过理由**: 表名规范正确、测试文件基本齐全、技术决策相对清晰、核心Requirement覆盖较完整。

**必须修复项（合入前）**:
1. 统一修正API路径为RESTful复数风格，避免RPC动作后缀
2. 修复design.md重复Decision D1问题
3. 在specs中补充完整API契约表格（至少包含核心列表/详情/创建接口）
4. 统一Flyway版本号命名为V3（与现有V1/V2递增一致）
