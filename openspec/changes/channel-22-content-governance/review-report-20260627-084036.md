# channel-22-content-governance 规范审核报告

**审核时间**: 2026-06-27 08:40:36
**Change类型**: 后端 (use-tdd-plan)
**审核范围**: proposal.md, design.md, plan.md, specs/ (5个spec), tasks.md

---

## 一、维度评分

| 维度 | 得分 | 问题数 | 主要问题 |
|------|------|--------|----------|
| 1. 完整性 | 5/10 | 6 | specs章节不完整，API契约表格缺失，VO定义不明确 |
| 2. 一致性 | 4/10 | 5 | 表名plan与design不一致，实体类名重复（两个GovernanceLog），SQL与JeecgBoot主键规范冲突 |
| 3. 可实现性 | 5/10 | 4 | 定时发布调度框架未确定（Open Question），依赖的内容发布服务接口未定义 |
| 4. 可测试性 | 4/10 | 3 | Scenario缺少Given前置条件，TDD配对不完整 |
| 5. 接口契约 | 3/10 | 7 | 无完整的req/vo/错误码/鉴权定义，API路径未在specs中明确列出 |
| 6. 边界覆盖 | 4/10 | 8 | 缺少并发、事务边界、Redis降级、Flyway回滚、内容已存在重复添加等边界 |
| 7. API命名规范 | 3/10 | 4 | 路径使用单数名词（/channel/publish），动作路径（/add-existing），非RESTful风格 |
| 8. 存量API兼容 | 6/10 | 2 | 未盘点已有内容发布API，可能路径冲突 |
| 9. 跨端一致性 | 5/10 | 2 | 未找到配对的前端change（无channel-22-frontend） |
| 10. 依赖分析 | 5/10 | 3 | 依赖EPIC-20/21的成员角色、禁言服务接口未明确引用 |
| 11. 数据表审核 | 3/10 | 8 | 表名无content_前缀、主键类型错误（VARCHAR(36)非VARCHAR(32)）、字段重复（created_by/create_by）、缺少软删除、外键隐含依赖 |
| 12. Plan审核 | 4/10 | 5 | Plan未覆盖所有Requirement，TDD测试代码是placeholder，分层顺序不完全正确 |

**综合得分**: 51/120 = **42.5%**

---

## 二、问题清单

### BLOCK级问题 (必须修复，门禁不通过)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| BLOCK-01 | 数据表审核 | plan.md:38-56, V1__channel_content_governance.sql:2 | **表名无content_前缀，与EPIC-20命名规范不一致** | 表名混乱：EPIC-20使用`content_channel`，本change使用`channel_content_publish`，无统一前缀 | 统一使用`content_`前缀，改为`content_channel_publish`、`content_channel_review`等 |
| BLOCK-02 | 数据表审核 | plan.md:39, SQL:3 | **主键类型VARCHAR(36)不符合JeecgBoot规范** | JeecgEntity使用ASSIGN_ID雪花算法生成32位ID，VARCHAR(36)浪费空间且与现有表不一致 | 改为VARCHAR(32)，与content_channel表保持一致 |
| BLOCK-03 | 数据表审核 | SQL:122-123 | **ChannelAnnouncement表存在重复字段created_by/create_by** | 字段冗余，JeecgEntity已有create_by字段，额外定义created_by导致歧义 | 删除多余的created_by字段，使用基类的create_by |
| BLOCK-04 | 一致性 | design.md:58, SQL:97, 实体目录 | **治理日志表名和实体类不一致**：design定义`channel_governance_log`，SQL实际是`channel_content_governance_log`，实体目录同时存在`ChannelGovernanceLog.java`和`ChannelContentGovernanceLog.java` | 代码混乱，Mapper引用冲突 | 统一命名，删除重复实体 |
| BLOCK-05 | 接口契约 | specs/* (5个spec) | **API契约完全缺失**：无任何spec定义HTTP method、路径、请求体、响应体、错误码 | 前后端无法对齐，Controller实现无依据 | 在每个spec中补充完整的API契约表格 |
| BLOCK-06 | API命名规范 | design.md:96-99, plan.md:1060 | **API路径使用单数名词+RPC风格**：`/api/v1/content/channel/publish`、`/add-existing` | 不符合RESTful规范和项目约定（应使用复数资源） | 改为`/api/v1/content/channels/{channelId}/publications`等复数风格 |

### FLAG级问题 (建议修复，不阻塞但有风险)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| FLAG-01 | 完整性 | specs/channel-publishing/spec.md:31-38 | **频道数量上限N未定义具体值** | 实现无依据，硬编码风险 | 明确默认值（如5个）和配置方式 |
| FLAG-02 | 可实现性 | design.md:192, Open Questions | **定时发布调度框架未确定**（XXL-Job或Spring Scheduled） | 调度实现方式不明确，影响定时发布可靠性 | 选定框架并补充到design.md |
| FLAG-03 | 边界覆盖 | specs/* | **缺少多频道发布的事务边界定义** | 部分成功部分失败时数据一致性无法保证 | 明确每个频道独立事务，定义失败重试策略 |
| FLAG-04 | 可测试性 | plan.md中的测试代码 | **TDD测试是简化placeholder**：无真实Mock依赖、无边界case、仅验证happy path | 测试覆盖率无法达到80%目标，代码质量无保障 | 补充完整的单元测试和集成测试 |
| FLAG-05 | Plan审核 | plan.md | **Plan未覆盖"已发布内容添加到频道"的完整TDD流程** | 任务7在plan中无对应实现步骤 | 补充AddExistingContent的TDD步骤 |
| FLAG-06 | 数据表审核 | SQL所有表 | **所有表缺少软删除字段（del_flag）** | 数据无法软删除，与JeecgBoot整体模式不一致 | 所有表添加del_flag TINYINT默认0 |

### ADVISORY级问题 (建议改进)

| 编号 | 维度 | 位置 | 描述 | 影响 | 修复建议 |
|------|------|------|------|------|----------|
| ADV-01 | 跨端一致性 | openspec/changes/ | **未找到channel-22-content-governance-frontend配对change** | 前后端并行开发无对齐依据 | 创建前端配对change |
| ADV-02 | 依赖分析 | design.md:3 | **依赖的EPIC-21禁言/黑名单服务接口未明确** | 权限校验无法正确调用已有服务 | 明确引用ChannelMuteService等已有接口 |
| ADV-03 | 完整性 | proposal.md:74 | **前端能力描述过于笼统** | 前端实现无具体指导 | 补充前端组件设计要点 |

---

## 三、量化指标

| 指标 | 值 | 说明 |
|------|-----|------|
| PRD AC覆盖率 | 40% | Success Criteria共6项，仅2项有可验证的Scenario覆盖 |
| API契约完整率 | 0% | 5个spec无1个定义完整API契约 |
| 边界条件覆盖率 | 30% | 列出的10类边界仅覆盖3类（null/超长/权限不足） |
| TDD配对率 | 20% | tasks标记的11项测试任务，plan中仅给出4个简化测试模板 |
| Scenario完整率 | 55% | 共22个Scenario，多数缺少Given前置条件 |
| 数据表合规率 | 25% | 7张表仅0张完全符合命名/字段/索引规范 |

---

## 四、门禁判定

**❌ BLOCKED (不通过)**

**阻断原因**: 存在6个BLOCK级问题，涉及数据表规范、API契约、一致性等核心问题，必须修复后方可进入实现。

**最小修复清单**:
1. 统一表名前缀为`content_`，主键改为VARCHAR(32)
2. 删除ChannelAnnouncement重复字段，所有表添加软删除
3. 统一GovernanceLog命名，删除重复实体
4. 在specs中补充完整API契约表格
5. 修正API路径为RESTful复数风格
