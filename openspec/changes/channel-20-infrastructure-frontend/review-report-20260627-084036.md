# OpenSpec 规范文档审核报告

**Change**: channel-20-infrastructure-frontend  
**审核时间**: 2026-06-27 08:40:36  
**Schema 类型**: spec-driven  
**配对后端 Change**: channel-20-infrastructure

---

## 评分总览

| 审核维度 | 得分 | 级别 |
|---------|------|------|
| 完整性 | 7/10 | FLAG |
| 一致性 | 8/10 | FLAG |
| 可实现性 | 8/10 | FLAG |
| 可测试性 | 6/10 | FLAG |
| 接口契约 | 6/10 | BLOCK |
| 边界覆盖 | 5/10 | BLOCK |
| API 命名规范 | 7/10 | FLAG |
| 存量 API 兼容 | 9/10 | ADVISORY |
| 跨端一致性 | 6/10 | FLAG |
| 依赖分析 | 8/10 | ADVISORY |

**综合得分**: 70/100

---

## 详细问题清单

### 1. 完整性问题 (7/10)

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| design.md 第29-33行 | 路由路径定义错误：文档写 `/api/v1/content/channel/create`，但项目使用 BACK 权限模式，实际前端路由应为 `/content/channel/create`（无 `/api/v1` 前缀） | 导致路由跳转 404 | 修正路由路径，与实际前端路由规范一致 |
| design.md 第67行 | 缺少 `model/channelModel.ts` 的详细字段定义说明，仅提及文件位置 | 前后端字段对齐困难 | 在 design.md 中补充核心 VO/Req 的字段对照表 |
| specs/channel-creation/spec.md | 缺少"已达创建上限"的接口契约说明，未明确前端如何获取上限数量 | 无法实现上限提示 | 补充上限检查的 API 或从用户信息中获取的字段说明 |
| design.md 第13行 | Pinia Store 定义不完整，未说明是否需要异步 action（如 fetchChannel、fetchList） | Store 使用方式不明确 | 补充 Store 的 action 定义，说明哪些操作需要封装异步请求 |

#### ADVISORY 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| specs/ | 缺少组件级 spec，如 ChannelForm、ReviewDiffViewer 的 props/emits 定义 | 组件实现自由度过大 | 补充关键共用组件的接口定义 |
| design.md | 未提及国际化（i18n）策略 | 后续国际化改造困难 | 明确文案是否硬编码或使用 i18n |

---

### 2. 一致性问题 (8/10)

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| proposal.md 第40-55行 vs design.md 第92-117行 | proposal.md 中列出 15 个接口，但 design.md 中列出 18 个接口（多出隐私设置、加入方式、审核详情），且 design.md 表格第83行 Controller 前缀与 proposal 不一致 | 接口数量不统一，实现时混淆 | 统一两个文档的 API 列表，删除本次 change 范围外的 API（隐私/加入方式属于 EPIC-21） |
| tasks.md 第1.7-1.8行 vs design.md 第29行 | 路由路径不一致：tasks.md 写 `/api/v1/content/channel/*`，实际前端页面使用 `/content/channel/*` | 路由实现与文档不符 | 统一为前端路由路径（不含 /api/v1 前缀） |

#### ADVISORY 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| specs/ | 各 spec 中 API 路径引用格式不统一，有的用反引号，有的用粗体 | 阅读体验不一致 | 统一 API 路径引用格式 |

---

### 3. 可实现性问题 (8/10)

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| design.md 第61-63行 | ReviewDiffViewer 组件设计过于简略，未说明 diff 算法实现方式，仅说"基于字符串对比" | 文本 diff 实现效果难以保证 | 补充 diff 对比的具体方案：是否使用 diff 库、对比粒度（字符/词/行） |
| design.md 第120行 | "不引入第三方 diff 库"的决策缺乏评估依据 | 复杂文本 diff 效果差 | 评估 jsdiff 等轻量库的引入成本 |
| specs/channel-transfer/spec.md | 用户搜索 API 未在文档中定义，design.md 中也未提及 | 转让功能无法完整实现 | 补充用户搜索的 API 路径和参数（应复用已有用户搜索接口） |

#### ADVISORY 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| design.md | 未提及图片上传复用项目现有 Upload 组件的具体方式（已有 JUpload 组件） | 可能重复实现上传逻辑 | 明确复用 `src/components/Upload` 组件 |
| design.md | 未提及 useTable hook 的使用方式，项目已有 `useListPage` 等封装 | 表格实现不统一 | 建议复用项目现有表格 hooks |

---

### 4. 可测试性问题 (6/10)

#### BLOCK 级别

无

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| specs/ 全部 | Scenario 中缺少明确的 Given-When-Then 结构，部分 Scenario 只有一句话描述 | 测试用例编写困难 | 按 GWT 格式重写关键 Scenario，明确前置条件 |
| specs/channel-creation/spec.md | 缺少网络超时、接口报错等异常场景的 Scenario | 异常处理无测试依据 | 补充异常场景的 Scenario |
| specs/review-queue/spec.md | 并发审核防护的 Scenario 未说明后端返回的具体错误码/消息 | 前端无法识别并发错误 | 补充并发审核时后端的响应格式 |
| design.md | 未定义 loading/error/success 状态的统一处理模式 | 各页面加载状态不一致 | 补充异步状态处理规范 |

---

### 5. 接口契约问题 (6/10)

#### BLOCK 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| proposal.md 第48-49行 vs API 实现 | 确认转让和拒绝转让的 API 路径错误：文档写 `/api/v1/content/channels/transfer/{transferId}/confirm`，实际应为 `/api/v1/content/channels/{id}/transfer/{transferId}/confirm`（缺少 channels/{id}/ 前缀） | 调用 404 | 修正 API 路径，确保以 `/api/v1/content/channels/` 开头 |
| specs/admin-channel-management/spec.md 第6行 | 后台频道列表 API 路径不明确，文档说"已存在"但未给出具体路径；前端 API 封装中缺少 `getAdminChannelList` 方法 | 后台管理列表无法正常分页查询 | 补充后台专用列表 API 路径，在 API 封装中添加对应方法 |

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| specs/channel-transfer/spec.md | 转让相关 VO 字段未明确定义：transferId、fromUserId、toUserId 等字段与后端是否一致？ | 字段映射错误 | 补充转让相关的 VO 字段定义，与后端对齐 |
| specs/review-queue/spec.md | 审核操作缺少"退回修改"的 API 参数说明（action 枚举值、note 字段要求） | 退回修改功能无法实现 | 补充 ReviewActionType 枚举：APPROVE/REJECT/RETURN，以及 note 的必填规则 |
| design.md 第82-88行 | API 前缀对照表中 Controller 前缀列有错误：如 `/api/v1/channels` 应为 `/api/v1/content/channels` | API 封装时路径错误 | 修正表格中的前缀路径，统一为 `/api/v1/content/` 开头 |

---

### 6. 边界覆盖问题 (5/10)

#### BLOCK 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| 全部 specs | 未提及重复提交防护策略 | 用户快速双击可能导致重复创建/转让/删除 | 补充防重复提交方案：使用 `useLockFn` hook 或按钮 loading 状态 |
| specs/channel-creation/spec.md | 缺少表单中途离开的未保存提示 | 用户误操作丢失表单数据 | 补充 beforeunload 和路由离开守卫的处理说明 |
| design.md | 未提及网络超时的 UI 反馈机制 | 请求超时用户无感知 | 补充网络超时、断网状态下的 UI 处理规范 |
| specs/ | 未明确 Token 过期时的页面状态处理（如正在编辑表单时 Token 过期） | Token 过期跳转登录页后表单数据丢失 | 补充 Token 过期场景下的用户体验说明 |

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| specs/channel-list/spec.md | 列表分页边界未说明：最后一页、空页、页码超出范围 | 分页异常无处理 | 补充分页边界场景 |
| specs/channel-creation/spec.md | 图片上传失败、上传中状态未说明 | 上传体验差 | 补充上传过程中的各种状态处理 |
| specs/review-queue/spec.md | 审核列表超 24h 高亮的计算方式未说明（前端计算还是后端返回 isTimeout 字段） | 超时判断不一致 | 明确是后端返回 waitDuration 由前端计算，还是后端直接返回 isTimeout 标记 |
| specs/ 全部 | 缺少响应式断点的具体定义（何时切换为移动端布局） | 移动端适配效果不一致 | 明确响应式断点：sm/md/lg 对应的布局变化 |

---

### 7. API 命名规范问题 (7/10)

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| proposal.md 第54-55行 | 审核相关 API 路径为单数形式 `/api/v1/content/channel/review/*`，而其他 API 为复数 `/api/v1/content/channels/*`，命名不一致 | 路径风格不统一 | 与后端确认是否统一为复数形式；如后端不可修改，需在文档中明确说明这是特例 |
| API 封装 index.ts 第76行 | `confirmTransfer` 和 `rejectTransfer` 使用 `Api.detail + 'transfer/' + transferId` 拼接，结果为 `/api/v1/content/channels/transfer/{id}/confirm`，缺少 `{id}/` 路径段 | API 路径错误 | 修正路径拼接逻辑：应为 `Api.detail + id + '/transfer/' + transferId + '/confirm'` |

---

### 8. 存量 API 兼容 (9/10)

#### ADVISORY 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| design.md | 未提及是否复用已有用户相关 API（如用户搜索、用户信息获取） | 可能重复封装已有 API | 补充说明需要复用的存量 API 清单 |
| API 封装 | createSystemChannel 和 reviewChannel 应确认是否有对应的后台专用 API 前缀（/admin/） | 可能存在权限问题 | 验证后台 API 是否需要特殊前缀或 header |

---

### 9. 跨端一致性问题 (6/10)

#### FLAG 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| model/channelModel.ts 第5行 vs 后端 design | 前端 ChannelStatus 使用大写下划线（`DELETE_COOLING`），后端使用数字枚举（0-5），需确认后端返回的是字符串还是数字 | 状态判断逻辑错误 | 与后端确认状态枚举的序列化格式：是返回字符串还是数字，前端应保持一致 |
| model/channelModel.ts 第20-29行 | ChannelVO 字段与后端 ChannelVO 未逐项对齐：如后端是否有 categoryId 还是只返回 categoryName、orgId 是否必填等 | 字段缺失或多余 | 与后端 VO 字段一一核对，补充缺失字段 |
| specs/channel-transfer/spec.md | 转让状态枚举（PENDING/ACCEPTED/REJECTED/EXPIRED）未与后端对齐 | 状态显示错误 | 确认后端转让状态枚举值 |
| 后端 specs/channel-review/spec.md vs 前端 specs/review-queue/spec.md | 后端有 review detail API（`GET /review/detail/{id}`），但前端 API 封装中缺少此方法 | 审核详情无法获取 | 在 API 封装中补充 `getReviewDetail` 方法 |

---

### 10. 依赖分析 (8/10)

#### ADVISORY 级别

| 位置 | 问题描述 | 影响 | 修复建议 |
|------|---------|------|---------|
| design.md | 未列出需要新增的公共组件具体路径：文档说"10 个共用组件"但只列出 3 个（ChannelForm、ChannelTypeTag、ChannelStatusTag、ReviewDiffViewer） | 组件拆分不明确 | 补充所有共用组件清单和存放位置 |
| design.md | 未确认项目是否已有 diff 相关工具函数 | 重复实现 | 检查项目 utils 目录是否已有文本对比工具 |

---

## BLOCK 级问题汇总（必须修复）

1. **接口路径错误**: 转让确认/拒绝 API 路径缺少 `{id}/` 前缀 (index.ts:76,80)
2. **后台列表 API 缺失**: admin 页面缺少专用列表 API 方法 (api/index.ts)
3. **缺少重复提交防护**: 所有关键操作未说明防重复提交策略
4. **未保存提示缺失**: 创建/编辑表单中途离开无提示
5. **网络超时 UI 缺失**: 未定义网络异常的用户反馈
6. **Token 过期场景缺失**: 操作中 Token 过期的处理未说明

---

## 审核结论

**状态**: NEEDS_REVISION

本 Change 文档整体框架完整，proposal/design/specs/tasks 齐全，但存在多个 BLOCK 级问题需要修复：
1. API 路径定义有误（转让相关路径拼接错误）
2. 后台专用 API 缺失
3. 边界场景覆盖严重不足（网络异常、重复提交、未保存提示等）
4. 跨端字段枚举未与后端对齐

建议修复上述 BLOCK 和 FLAG 问题后再进入实现阶段。
