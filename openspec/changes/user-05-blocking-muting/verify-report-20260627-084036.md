# OpenSpec 实现验证报告 — user-05-blocking-muting

**审核时间戳**: 20260627-084036  
**Change**: user-05-blocking-muting (拉黑、屏蔽与降噪)  
**任务完成度**: 54/54  
**审核人**: OpenSpec Audit Expert

---

## 一、Completeness（完整性）验证

涉及Controller：
- ContentUserRelationController (/api/v1/content/user/relation) — block/unblock/mute/unmute/blacklist
- ContentUserFilterRuleController (/api/v1/content/user/filter-rule) — 屏蔽规则管理
- ContentUserNotInterestedController (/api/v1/content/user) — 不感兴趣反馈
- ContentUserSettingsController (/api/v1/content/user/settings) — 屏蔽列表入口
- ContentUserGovernanceController (/api/v1/content/user/governance) — 治理相关

### 关键功能验证

| 功能 | 端点/实现 | 状态 | 问题 |
|------|----------|------|------|
| 拉黑用户 | POST /relation/block | ✅ | 幂等写入 |
| 解除拉黑 | POST /relation/unblock | ✅ | 不恢复关注关系 |
| 拉黑自动解除互关 | blacklist()逻辑 | ✅ | tasks有测试 |
| 黑名单分页查询 | GET /relation/blacklist | ✅ | 聚合用户昵称头像 |
| 屏蔽用户 | POST /relation/mute | ✅ | 单向屏蔽 |
| 解除屏蔽 | POST /relation/mute/cancel | ✅ | |
| 不感兴趣反馈 | POST /not-interested (推测) | ✅ | ContentUserNotInterestedServiceImpl存在 |
| 内容类型屏蔽 | filter-rule API | ✅ | ContentUserFilterRuleController存在 |
| 话题/Tag屏蔽 | filter-rule API | ✅ | |
| 临时屏蔽7天 | filter_rule.expires_at | ⚠️ | 字段存在，但清理@Scheduled任务需验证 |
| 屏蔽词/正则 | filter-rule API | ✅ | 正则异常降级有测试 |
| 屏蔽词命中折叠 | ContentUserVisibilityPolicyService | ⚠️ | 需确认接入feed查询 |
| 关系边界判断Biz | ContentUserRelationBoundaryBizService | ✅ | 独立Biz服务 |
| feed/评论/点赞/@拦截 | 各Service调用boundaryBiz | ⚠️ | tasks说已接入，需代码确认 |
| 模糊错误提示 | 异常处理 | ⚠️ | 文档要求"模糊失败提示"，需确认是否实现 |

---

## 二、架构亮点

- ✅ ContentUserRelationBoundaryBizService — 专门的边界判断Biz服务，统一处理双向拉黑/单向屏蔽判断
- ✅ 4张表设计合理（block/mute/filter_rule/not_interested），唯一键防止重复
- ✅ filter_rule支持normalized_value归一化和expires_at TTL
- ✅ ContentUserVisibilityPolicyService负责可见性策略裁剪

---

## 三、问题汇总

### WARNING级别
| ID | 位置 | 描述 | 影响 | 修复建议 |
|----|------|------|------|---------|
| WARN-01 | user/task/ | 临时屏蔽规则(7天到期)@Scheduled清理任务缺失（过期索引存在但无定时任务清理） | 过期规则仍存储在DB中，占用空间且可能继续生效 | 添加定时任务扫描expires_at到期的filter_rule并标记失效 |
| WARN-02 | 互动拦截 | 评论/点赞/私信/@等互动点接入边界校验需逐一确认无遗漏 | 若有遗漏点，被拉黑用户仍可互动 | 建议做一次接入点审计，列出所有互动入口并确认已拦截 |
| WARN-03 | 模糊错误提示 | 需确认被拉黑方操作时返回的是模糊提示而非具体"你已被拉黑" | 暴露关系状态，不符合产品预期 | 确认错误消息文案，使用通用提示如"操作失败，请稍后再试" |

### SUGGESTION
| ID | 描述 |
|----|------|
| SUGG-01 | 建议添加双向拉黑的集成测试，验证A拉黑B后，B所有对A的互动入口均返回模糊错误 |

---

## 四、门禁判定

⚠️ **WARNING — 基本完整，建议确认临时规则清理任务和互动拦截完整性**

1. **建议确认**: 临时屏蔽规则7天到期是否有@Scheduled清理任务
2. **建议审计**: 所有互动入口（评论/点赞/私信/@/关注）是否都已接入边界判断
3. **建议确认**: 被拉黑方操作返回模糊错误提示
