## 1. 数据结构与迁移

- [x] 1.1 编写拉黑、屏蔽用户、屏蔽规则、不感兴趣反馈表 Flyway migration
- [x] 1.2 添加 migration 表结构、唯一索引、默认值和回滚脚本校验测试
- [x] 1.3 创建新增表对应 entity、mapper、service 基础类
- [x] 1.4 添加 mapper 启动加载、基础 CRUD 和唯一约束测试
- [x] 1.5 定义拉黑、屏蔽、过滤规则状态枚举和规则类型枚举
- [x] 1.6 添加枚举空值、非法值、越界值转换测试

## 2. 拉黑关系与黑名单

- [x] 2.1 实现拉黑用户接口和幂等写入逻辑
- [x] 2.2 添加 targetUserId 为 null、空值、越界、自身、未知用户和重复拉黑测试
- [x] 2.3 实现拉黑后解除双方关注关系和缓存清理
- [x] 2.4 添加互关解除、单向关注解除、解除失败事务回滚测试
- [x] 2.5 实现黑名单倒序分页查询和用户展示字段聚合
- [x] 2.6 添加黑名单排序、分页边界、用户昵称头像展示测试
- [x] 2.7 实现解除拉黑逻辑且不恢复关注关系
- [x] 2.8 添加解除拉黑目标空值、未知关系、反向仍拉黑、关注不恢复测试

## 3. 拉黑边界接入

- [x] 3.1 实现关系边界 biz 服务，提供双向拉黑判断和互动断言
- [x] 3.2 添加正向拉黑、反向拉黑、双方未拉黑、空 userId 边界测试
- [x] 3.3 将主页访问接入拉黑可见性判断
- [x] 3.4 添加发起方访问被拉黑主页、被拉黑方访问发起方主页、无拉黑正常访问测试
- [x] 3.5 将推荐流、关注流、评论列表和动态列表接入拉黑作者过滤
- [x] 3.6 添加双向过滤、分页数量补齐、空列表、越界分页测试
- [x] 3.7 将评论、点赞、私信和 @ 通知接入互动拦截
- [x] 3.8 添加评论失败、点赞失败、私信失败、@ 不通知、模糊错误提示测试

## 4. 屏蔽用户与信息流降噪

- [x] 4.1 实现屏蔽用户接口和屏蔽用户关系写入
- [x] 4.2 添加 mutedUserId 为 null、空值、越界、自身、未知用户和重复屏蔽测试
- [x] 4.3 将推荐流和关注流接入屏蔽用户过滤
- [x] 4.4 添加屏蔽用户内容不展示、主页仍可访问、关注关系保留、单向屏蔽测试
- [x] 4.5 实现取消屏蔽用户逻辑
- [x] 4.6 添加取消屏蔽目标空值、未知关系、取消后信息流恢复测试

## 5. 不感兴趣与话题类型过滤

- [x] 5.1 实现”不感兴趣”反馈记录接口
- [x] 5.2 添加 contentId 和 contentType 的 null、空值、越界、未知内容、非法类型测试
- [x] 5.3 实现内容类型屏蔽规则保存和信息流过滤
- [x] 5.4 添加内容类型 null、空值、越界、非法枚举、过滤生效测试
- [x] 5.5 实现话题/Tag 屏蔽规则保存和信息流过滤
- [x] 5.6 添加话题 null、空值、越界、非法格式、包含话题过滤测试
- [x] 5.7 实现临时话题屏蔽、到期失效和提前取消
- [x] 5.8 添加 7 天有效期、过期恢复、提前取消、重复取消测试

## 6. 屏蔽词与正则规则

- [x] 6.1 实现关键词屏蔽规则保存、删除和列表查询
- [x] 6.2 添加关键词 null、空值、越界、重复保存、删除后失效测试
- [x] 6.3 实现正则屏蔽规则保存前校验和异常降级
- [x] 6.4 添加正则 null、空值、越界、非法表达式、复杂度超限测试
- [x] 6.5 将标题和正文屏蔽词匹配接入信息流响应
- [x] 6.6 添加命中标题、命中正文、未命中、折叠提示、展开查看测试

## 7. 列表管理与用户教育

- [x] 7.1 实现隐私设置中的黑名单和屏蔽列表入口数据接口
- [x] 7.2 添加入口存在性、登录态缺失、空列表、分页边界测试
- [x] 7.3 实现屏蔽用户、话题、类型、临时规则、屏蔽词的统一列表查询
- [x] 7.4 添加各类规则混合展示、排序、过期规则隐藏测试
- [x] 7.5 实现单个取消和批量取消屏蔽规则
- [x] 7.6 添加批量 ids 为 null、空值、重复、越界、未知、非本人规则测试
- [x] 7.7 补齐拉黑、屏蔽、解除拉黑的确认文案和帮助说明响应
- [x] 7.8 添加拉黑确认、屏蔽确认、解除拉黑提示、帮助说明文案测试

## 8. API 文档与验证

- [x] 8.1 补齐 controller、req、vo、错误码和 Knife4j 中文说明
- [x] 8.2 添加 Controller WebMvc 成功响应、参数校验和错误码测试
- [x] 8.3 运行内容社区拉黑屏蔽相关单元测试、WebMvc 测试和 mapper 测试
- [x] 8.4 修复测试失败并复跑到通过
