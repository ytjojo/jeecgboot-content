# OpenSpec 漂移检测+架构审核报告 — user-05-blocking-muting

**审核时间戳**: 20260627-084036  
**Change**: user-05-blocking-muting (拉黑、屏蔽与降噪)  
**审核人**: OpenSpec Audit Expert

---

## 一、漂移总览

| 漂移类型 | 数量 | 说明 |
|---------|------|------|
| Controller跨change | 1 | ContentUserRelationController同时承载user-04关注和user-05拉黑/屏蔽 |
| 文档缺失 | 2 | plan.md缺失、design无Decision/API清单 |
| 定时任务待确认 | 1 | 临时屏蔽规则到期清理 |

---

## 二、API路径检查

✅ 路径前缀符合规范：
- /api/v1/content/user/relation — block/unblock/mute/unmute/blacklist
- /api/v1/content/user/filter-rule — 屏蔽规则CRUD
- /api/v1/content/user — not-interested
- /api/v1/content/user/settings — 屏蔽列表入口

无/sys/前缀混用。

---

## 三、架构评价

### 亮点
- **ContentUserRelationBoundaryBizService** — user包下唯一的Biz服务，专门处理拉黑/屏蔽双向边界判断，架构清晰
- **ContentUserVisibilityPolicyService** — 统一可见性策略服务，隐私裁剪和拉黑/屏蔽过滤统一入口
- **4张表设计合理** — block(双向)/mute(单向)/filter_rule(规则)/not_interested(反馈)职责分离，唯一键防重复
- **filter_rule归一化设计** — normalized_value字段支持大小写/格式归一化匹配

### 注意点
- ContentUserRelationController跨user-04(关注)和user-05(拉黑/屏蔽)两个change，属于功能内聚但跨change共享Controller，建议确认是否接受
- 临时屏蔽expires_at索引存在，但需确认是否有@Scheduled清理任务

---

## 四、问题与门禁

| 级别 | 数量 | 关键问题 |
|------|------|---------|
| BLOCK | 1 | plan.md缺失(共性) |
| WARNING | 1 | 临时规则清理任务待确认 |

⚠️ **FLAG — 质量较好，建议补全plan.md并确认定时任务**

user-05是5个change中架构最清晰的一个，边界判断BizService和可见性策略Service设计合理。
