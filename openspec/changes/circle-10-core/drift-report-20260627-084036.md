# OpenSpec 漂移检测 + 架构审核报告

**Change**: circle-10-core (圈子核心基础设施)
**审核时间**: 2026-06-27 08:40:36
**审核维度**: API路径漂移 / 字段漂移 / 分层架构 / 安全

---

## 审核得分: 90/100

| 维度 | 得分 | 漂移数 | 状态 |
|------|------|--------|------|
| API路径一致性 | 100/100 | 0 | ✅ |
| 字段一致性 | 85/100 | 2 | ⚠️ |
| 分层架构合规 | 90/100 | 1 | ⚠️ |
| 安全性 | 85/100 | 2 | ⚠️ |

---

## 1. API路径漂移检测

### ✅ 路径前缀合规
所有Controller均使用 `/api/v1/content/` 前缀：

| Controller | RequestMapping前缀 | 合规 |
|-----------|-------------------|------|
| CircleController | /api/v1/content/circle | ✅ |
| CircleMemberController | /api/v1/content/circle/member | ✅ |
| CircleSearchController | /api/v1/content/circle | ✅ |
| CircleGovernanceLogController | /api/v1/content/circle/governance-log | ✅ |

### ✅ HTTP方法合规
- 创建使用POST
- 更新使用PUT
- 查询使用GET
- 符合RESTful规范 ✅

---

## 2. 字段漂移检测

### 🔴 严重漂移 (0个)
无

### 🟡 中等漂移 (1个)

| ID | 位置 | 规范定义 | 实际实现 | 漂移类型 | 修复建议 |
|----|------|---------|---------|---------|---------|
| F-001 | design.md vs 代码 | plan.md设计有CircleDTO/CircleMemberDTO | 实际代码未实现DTO层，使用Entity/VO直接转换 | 实现漂移 | 若DTO确实不需要，更新design.md移除DTO相关描述；若需要则补全 |

### 🟢 轻微漂移 (1个)

| ID | 位置 | 规范定义 | 实际实现 | 漂移类型 | 修复建议 |
|----|------|---------|---------|---------|---------|
| F-002 | design.md File Structure | Mapper XML路径为 `mapper/api/v1/content/circle/` | 实际路径为 `mapper/content/circle/` | 文档漂移 | 更新design.md中的路径与实际一致 |

### ✅ 数据库字段对齐验证

| 表名 | 字段 | 设计 | 实际 | 状态 |
|------|------|------|------|------|
| content_circle | name | varchar(100), UNIQUE | varchar(100), UNIQUE | ✅ |
| content_circle | privacy_type | varchar(32) | varchar(32) | ✅ |
| content_circle | join_type | varchar(32) | varchar(32) | ✅ |
| content_circle | password_hash | varchar(255) | varchar(255) | ✅ |
| content_circle | member_count | int DEFAULT 1 | int DEFAULT 1 | ✅ |
| content_circle_member | role | varchar(32) | varchar(32) | ✅ |
| content_circle_member | status | varchar(32) | varchar(32) | ✅ |
| content_circle_member | mute_end_time | datetime | datetime | ✅ |
| content_circle_governance_log | action | varchar(32) | varchar(32) | ✅ |
| content_circle_governance_log | extra_data_json | text | text | ✅ |

---

## 3. 分层架构审核

### ✅ 合规项
1. **包结构**: controller/biz/service/mapper/entity/req/vo 完整分层 ✅
2. **依赖方向**: Controller → Biz → Service → Mapper，单向依赖 ✅
3. **Service接口+实现**: 每个Service都有Interface和Impl分离 ✅
4. **Entity规范**: 使用Lombok @Data，继承JeecgEntity（除CircleGovernanceLog） ✅
5. **Req/VO分离**: 请求对象和响应对象分离，不使用Entity作为入参/出参 ✅

### ⚠️ 架构问题 (1个)

| ID | 位置 | 问题描述 | 严重级别 | 修复建议 |
|----|------|----------|----------|----------|
| A-001 | CircleController.java:88-117 | myList方法直接在Controller中进行两次数据库查询并组装VO，业务逻辑泄漏到Controller层 | Medium | 将myList和publicList的查询组装逻辑下沉到CircleBiz或CircleService中 |

---

## 4. 安全性审核

### ✅ 已实现的安全措施

| 安全项 | 实现位置 | 状态 |
|--------|---------|------|
| 认证要求 | SecureUtil.currentUser()获取用户ID | ✅ |
| 权限检查 | Service层checkCreatorPermission等方法 | ✅ |
| 私有圈子不可搜索 | CircleSearchController过滤PRIVATE类型 | ✅ |
| SQL注入防护 | CircleSearchController转义特殊字符 | ✅ |
| 密码加密 | 使用BCrypt（design明确） | ✅ |
| 参数校验 | @Valid + @NotBlank/@NotNull/@Size | ✅ |

### ⚠️ 安全问题 (2个)

| ID | 位置 | 问题描述 | 严重级别 | 修复建议 |
|----|------|----------|----------|----------|
| S-001 | 多个Controller | 公开接口（public-list、search、announcement等）使用 `SecureUtil.currentUser().getId()` 获取用户ID，匿名访问时可能抛出NPE | High | 匿名接口使用try-catch或可选认证方式获取用户ID，未登录时返回null而非抛异常 |
| S-002 | CircleAnnouncementController.java:40 | 使用JwtUtil.getUserNameByToken(request)而非统一的SecureUtil，获取用户ID方式不一致 | Medium | 统一使用SecureUtil.currentUser()获取用户信息，保持认证方式一致 |

---

## 5. 设计文档与实现差异汇总

| 类别 | 设计文档 | 实际实现 | 差异类型 |
|------|---------|---------|---------|
| Flyway版本 | V3.9.1_63 | V3.9.1_64 | 文档未更新 |
| Mapper XML路径 | mapper/api/v1/content/circle/ | mapper/content/circle/ | 文档未更新 |
| DTO层 | CircleDTO/CircleMemberDTO | 未实现 | 实现简化 |
| CircleGovernanceLogController | 未在File Structure列出 | 已实现 | 文档遗漏 |
| detail接口 | design中写了/detail(query param) | 仅实现/{id}(path param) | 文档与实现不一致 |

---

## 6. 与其他change的边界检查

### ✅ 与circle-11边界清晰
- circle-10仅包含核心CRUD、成员管理、搜索
- 置顶/精华/公告/@/审核/举报均由circle-11实现，边界清晰 ✅

### ✅ 无跨change依赖问题
- circle-10不依赖未实现的接口
- 敏感词服务有降级方案 ✅

---

## 门禁判定

⚠️ **WARN (有条件通过)**
- API路径100%合规，无严重漂移
- S-001匿名访问NPE问题建议在上线前修复
- 文档漂移问题不影响功能，但建议同步更新以保持一致性
- A-001 Controller逻辑泄漏建议重构但不阻塞合入
