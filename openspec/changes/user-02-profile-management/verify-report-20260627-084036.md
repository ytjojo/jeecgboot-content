# OpenSpec 实现验证报告 — user-02-profile-management

**审核时间戳**: 20260627-084036  
**Change**: user-02-profile-management (用户资料管理与主页个性化)  
**任务完成度**: 55/58  
**审核人**: OpenSpec Audit Expert

---

## 一、Completeness（完整性）验证

### Controller端点实现验证

| 端点 | Controller方法 | 实现状态 | 备注 |
|------|--------------|---------|------|
| GET /detail | getProfile() | ✅ | 实现viewer视角裁剪 |
| POST /update | updateProfile() | ✅ | 统一更新端点 |
| POST /review/handle | handleReview() | ✅ | 后台审核处理 |
| POST /privacy/update | updatePrivacy() | ✅ | 返回String而非VO（与design有小差异） |
| POST /homepage/update | updateHomepage() | ✅ | 通过homepageService |
| POST /homepage/defaults/restore | restoreHomepageDefaults() | ✅ | |
| GET /homepage/modules | listHomepageModules() | ✅ | |
| GET /badge/list | listBadges() | ✅ | |
| GET /badge/detail | badgeDetail() | ✅ | |
| GET /history/list | listHistory() | ✅ | 按historyType区分 |
| POST /history/restore | restoreHistory() | ✅ | |

**端点完整性**: ✅ 11/12个端点实现（proposal说12个，实际11个，计数问题）

### Service层实现验证

| Service | 实现类 | 状态 | 问题 |
|---------|--------|------|------|
| IContentUserProfileService | ContentUserProfileServiceImpl | ✅ | 直接注入5个Mapper（架构问题） |
| IContentUserHomepageService | ContentUserHomepageServiceImpl | ✅ | 存在 |
| IContentUserVerificationBadgeService | ContentUserVerificationBadgeServiceImpl | ✅ | 存在 |
| IContentUserProfileHistoryService | ContentUserProfileHistoryServiceImpl | ✅ | 存在 |
| IContentUserVisibilityPolicyService | ContentUserVisibilityPolicyServiceImpl | ✅ | 存在 |

### 关键功能验证

| 功能 | 实现证据 | 状态 | 问题 |
|------|---------|------|------|
| 资料VO字段可见性裁剪 | ContentUserProfileVO.from(...) | ✅ | 集中裁剪逻辑存在 |
| 敏感词/AI审核适配 | IContentUserProfileAuditAdapter | ⚠️ | 接口存在，但Noop实现？需确认 |
| 频控逻辑 | PROFILE_DAILY_LIMIT/PRIVACY_HOURLY_LIMIT常量 | ⚠️ | 常量存在，tasks标记8.1未完成，需确认是否实际生效 |
| 180天历史清理定时任务 | 搜索@Scheduled未发现profile history相关 | ❌ CRITICAL | 无定时任务执行过期清理 |
| 缓存失效 | invalidateProfileCache() | ✅ | RedisTemplate存在，失效逻辑存在 |
| 历史记录恢复复用更新流程 | restoreHistory() | ✅ | 委托给profileService |
| 主页默认配置恢复 | restoreDefaults() | ✅ | DEFAULT_MODULES常量存在 |
| 认证徽章聚合 | listVisibleBadges() | ✅ | verificationBadgeService实现 |

---

## 二、Correctness（正确性）验证

### 字段类型一致性检查

| Entity字段 | SQL类型 | Java类型 | 一致性 |
|-----------|---------|---------|--------|
| ContentUserProfile.profileCompletionState | varchar(32) | String | ✅ |
| ContentUserProfile.profileReviewStatus | varchar(32) | String | ✅ |
| ContentUserProfile.profileVersion | int | Integer | ✅ |
| ContentUserHomepageModule.visible | tinyint(1) | Boolean | ✅ |
| ContentUserHomepageModule.sortOrder | int | Integer | ✅ |
| ContentUserVerificationBadge.status | varchar(32) | String | ✅ |
| ContentUserProfileHistory.expired | tinyint(1) | Boolean | ✅ |
| ContentUserProfileHistory.expiresAt | datetime | Date/LocalDateTime | 需确认 |

### 参数校验验证

| Req类 | @Valid注解 | 字段校验 | 状态 |
|-------|-----------|---------|------|
| ContentUserProfileUpdateReq | ✅ | @NotBlank/@Size/@Pattern/@Past | ✅ |
| ContentUserPrivacyUpdateReq | ✅ | @Pattern校验visibility枚举 | ✅ |
| ContentUserHomepageUpdateReq | ✅ | @Size/@Pattern | ✅ |
| ContentUserReviewHandleReq | ✅ | 存在 | ✅ |

### 架构分层检查

```
实际架构: Controller → Service → Mapper (3层，无独立Biz层)
对比auth模块: Controller → BizService → Service → Mapper (4层)
```

| Service | Mapper注入数量 | 问题级别 |
|---------|--------------|---------|
| ContentUserProfileServiceImpl | 5个Mapper直接注入 | WARNING |
| ContentUserHomepageServiceImpl | 待确认 | - |
| ContentUserProfileHistoryServiceImpl | 待确认 | - |

> 注：user包整体采用3层架构（无独立Biz层），与auth模块（4层）不一致，但属于模块内一致性选择。需确认是否是有意为之。

---

## 三、Coherence（一致性）验证

### API路径一致性

| 端点前缀 | 实际使用 | 一致性 |
|---------|---------|--------|
| /api/v1/content/user/profile | 全部使用 | ✅ |
| 使用/sys/前缀 | 无 | ✅ (符合user模块使用/content/前缀的实际情况) |
| 写操作用POST | 全部写操作POST | ✅ 符合AGENTS.md规范 |

### 与前端配对change的跨端一致性检查

| 检查项 | 状态 | 备注 |
|--------|------|------|
| 前端change存在 | 待确认 | user-02-profile-management-frontend |
| 字段名对齐 | 未验证 | 需读取前端spec |

### 与EPIC-01依赖一致性

| 依赖项 | 实现状态 | 备注 |
|--------|---------|------|
| userId获取 | ✅ | 使用@RequestParam userId（前端传参） |
| SystemUserAccountGateway | ✅ | 网关层存在 |

---

## 四、问题汇总

### CRITICAL级别
| ID | 位置 | 描述 | 影响 | 修复建议 |
|----|------|------|------|---------|
| CRIT-01 | user/task/ | 180天资料历史过期清理@Scheduled定时任务缺失 | content_user_profile_history表会无限增长，过期数据无法自动清理 | 添加@Scheduled定时任务扫描expires_at到期的历史记录并标记expired或清理 |

### WARNING级别
| ID | 位置 | 描述 | 影响 | 修复建议 |
|----|------|------|------|---------|
| WARN-01 | ContentUserProfileServiceImpl.java:63-76 | 直接注入5个Mapper，无Biz层 | Service层承担了过多数据访问编排职责 | 参考auth模块添加Biz层，或确认3层架构是模块既定规范 |
| WARN-02 | tasks.md:79 | 资料更新频控(每日5次/每小时10次)标记未完成，但代码中有常量定义 | 常量可能未实际生效，存在被刷风险 | 确认频控逻辑是否已实现，若未实现要么补齐要么明确移除常量 |
| WARN-03 | ContentUserProfileController.java:78-83 | updatePrivacy返回Result<String>而非Result<ContentUserPrivacySettingVO> | 与design.md Delta端点设计不一致 | 改为返回VO，或更新design说明返回String的原因 |
| WARN-04 | IContentUserProfileAuditAdapter | 审核适配器接口存在但无真实实现（可能是Noop） | 敏感词和AI审核实际不生效 | 确认是Noop还是对接了真实审核服务，Noop需文档明确说明 |

### SUGGESTION级别
| ID | 描述 |
|----|------|
| SUGG-01 | 素材URL校验仅做@Pattern正则，未校验文件大小和格式（由前端+OSS保证，但服务端无二次校验） |
| SUGG-02 | /badge/detail接口未做badge所有权校验，任意用户可查看任意badge详情（若badge不含敏感信息则可接受） |

---

## 五、门禁判定

⚠️ **WARNING — 可测试但需修复1个CRITICAL问题**

1. **必须修复**: 添加资料历史180天过期清理@Scheduled定时任务
2. **必须确认**: 频控逻辑是否实际生效
3. **建议修复**: 确认架构分层规范（3层vs4层）并统一
4. **建议修复**: updatePrivacy返回类型对齐文档
