# OpenSpec 漂移检测+架构审核报告 — user-03-badges-points-growth

**审核时间戳**: 20260627-084036  
**Change**: user-03-badges-points-growth (勋章、积分与成长等级)  
**审核人**: OpenSpec Audit Expert

---

## 一、代码-文档漂移总览

| 漂移类型 | 数量 | 说明 |
|---------|------|------|
| 正向漂移 | 0 | - |
| 负向漂移 | 2 | 勋章过期/经验衰减定时任务缺失 |
| 字段命名/类型漂移 | 0 | 未发现明显字段类型不匹配 |
| API路径漂移 | 0 | 路径前缀一致 |
| 文档缺失 | 2 | design无Decision章节、无API清单、无回滚脚本 |

---

## 二、API路径一致性检查

所有端点使用`/api/v1/content/user/growth`前缀，✅ 符合预期，无/sys/前缀混用。

| 检查项 | 结果 |
|--------|------|
| 路径前缀正确 | ✅ |
| HTTP方法使用 | ✅ 写POST/读GET |
| 无/sys/前缀 | ✅ |

---

## 三、字段名/类型一致性检查

抽查主要Entity与SQL：
- reward_rule/reward_event/level_config/level_benefit_config/exchange_goods/exchange_order/feature_unlock/virtual_gift_record/growth_decay_state 表名一致
- 字段类型未发现明显不匹配（对比V55 SQL与Entity）

---

## 四、架构分层审核

| 层次 | 检查结果 |
|------|---------|
| Controller → Service | ✅ ContentUserGrowthController注入6个Service接口 |
| Service → Mapper | ⚠️ 3层架构，Service直接注入Mapper（与user模块一致，但与auth模块4层不一致） |
| Biz层 | ❌ 无独立Biz层编排服务 |

架构一致性：user模块整体采用3层架构，是模块内一致性选择，非bug。但跨模块(auth vs user)不一致，建议文档说明。

---

## 五、定时任务缺失问题

user-03缺失的定时任务：
1. **勋章过期回收任务** — 扫描content_user_badge_grant中expires_at到期的勋章，更新状态为EXPIRED，取消佩戴，发送通知
2. **经验衰减任务** — 每日扫描content_user_profile（或关联表）中last_login_time超过30天的用户，按规则衰减成长值，记录负向台账，触发降级保护检查

---

## 六、问题汇总与门禁

| 级别 | 数量 | 关键问题 |
|------|------|---------|
| CRITICAL | 2 | 勋章过期定时任务缺失、经验衰减定时任务缺失 |
| BLOCK | 1 | plan.md缺失 |
| WARNING | 3 | 无Decision章节、无API清单、缺V55回滚脚本 |
| ADVISORY | 2 | 事务注解验证、管理员接口权限校验 |

❌ **BLOCKED — 需修复2个CRITICAL定时任务缺失(共性问题)**

定时任务缺失是user-01~user-04的共性架构问题，建议统一在user/task/下创建：
- ContentUserAccountCancellationScheduler (user-01)
- ContentUserProfileHistoryCleanupScheduler (user-02)
- ContentUserBadgeExpireScheduler (user-03)
- ContentUserGrowthDecayScheduler (user-03)
- ContentUserSubscriptionDigestScheduler (user-04)
